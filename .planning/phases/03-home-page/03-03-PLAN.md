---
phase: 03-home-page
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - sections/featured-products.liquid
  - sections/new-arrivals.liquid
autonomous: true
requirements:
  - HOME-04
  - HOME-08

must_haves:
  truths:
    - "Featured products section pulls from a theme-editor-selected 'Best Sellers' collection"
    - "Product cards show image (3/4 aspect), title, and price"
    - "Quick-add button slides up from bottom of card on hover"
    - "New arrivals section pulls from a theme-editor-selected collection (newest products)"
    - "New arrivals has a 'View All New Arrivals →' CTA link below the grid"
    - "Both grids are 2 columns on mobile, 4 columns on desktop"
  artifacts:
    - path: "sections/featured-products.liquid"
      provides: "Best Sellers product grid section with quick-add hover"
      contains: "{% schema %}"
    - path: "sections/new-arrivals.liquid"
      provides: "New Arrivals product grid section with view-all CTA"
      contains: "{% schema %}"
  key_links:
    - from: "sections/featured-products.liquid"
      to: "Shopify collection"
      via: "section.settings.collection (type: collection schema setting)"
    - from: "sections/new-arrivals.liquid"
      to: "Shopify collection"
      via: "section.settings.collection (type: collection schema setting)"
---

<objective>
Build the Featured Products section (HOME-04) and New Arrivals section (HOME-08) as Shopify Liquid sections that pull live products from owner-selected collections.

Purpose: These are the primary product discovery sections on the home page — they surface Best Sellers and New Arrivals directly, driving add-to-cart behavior without requiring a full collection page visit.
Output: sections/featured-products.liquid, sections/new-arrivals.liquid
</objective>

<execution_context>
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\ROADMAP.md
@F:\CODING\BikinniSite\sections\announcement-bar.liquid

<interfaces>
<!-- Design tokens: bg-sand (HOME-04), bg-cream (HOME-08), text-deep, text-mid, font-display, font-body -->

<!-- Product card shared spec (UI-SPEC §HOME-04, §HOME-08 Interactive States): -->
<!-- Card wrapper: relative group overflow-hidden bg-cream -->
<!-- Image container: relative overflow-hidden aspect-[3/4] -->
<!-- Image: w-full h-full object-cover scale-100 group-hover:scale-105 transition-transform duration-500 -->
<!-- Card body: p-4 -->
<!-- Product title: font-body text-base font-semibold tracking-wide uppercase text-deep -->
<!-- Price: font-body text-sm font-normal text-mid -->
<!-- Quick-add overlay: absolute bottom-0 left-0 right-0 bg-deep text-cream py-3 text-center font-body text-xs tracking-widest uppercase translate-y-full group-hover:translate-y-0 transition-transform duration-200 -->
<!-- Quick-add label: "Quick Add" -->
<!-- Card hover shadow: group-hover:shadow-md transition-shadow duration-200 (apply to card wrapper) -->

<!-- Grid: grid grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6 -->

<!-- Section heading: font-display text-3xl md:text-4xl text-deep text-center mb-8 md:mb-12 -->

<!-- HOME-04 (Featured Products): -->
<!-- bg-sand py-12 md:py-16 -->
<!-- Heading: "Best Sellers" (owner-editable) -->
<!-- Collection: section.settings.collection — iterate collection.products limit: section.settings.product_count -->
<!-- Schema: collection picker + product_count (range 4–8 default 4) + heading text -->

<!-- HOME-08 (New Arrivals): -->
<!-- bg-cream py-12 md:py-16 -->
<!-- Heading: "New Arrivals" (owner-editable) -->
<!-- View all CTA: "View All New Arrivals →" href="{{ section.settings.collection.url }}" -->
<!-- CTA class: font-body text-sm font-semibold tracking-widest uppercase text-coral hover:text-deep transition-colors -->
<!-- Collection: section.settings.collection, sorted by published_at descending if collection allows -->
<!-- Schema: collection picker + product_count (range 4–8 default 4) + heading text + view_all_label text -->

<!-- Liquid product iteration pattern: -->
<!-- {% assign products = section.settings.collection.products | limit: section.settings.product_count %} -->
<!-- {% for product in products %} -->
<!-- {{ product.title }} -->
<!-- {{ product.price | money }} -->
<!-- {{ product.featured_image | image_url: width: 600 | image_tag: class: 'w-full h-full object-cover ...', loading: 'lazy' }} -->
<!-- {% endfor %} -->

<!-- Quick-add form: use a simple add-to-cart form pointing to /cart/add -->
<!-- <form action="/cart/add" method="post"> -->
<!--   <input type="hidden" name="id" value="{{ product.first_available_variant.id }}"> -->
<!--   <button type="submit" class="...">Quick Add</button> -->
<!-- </form> -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Featured Products section (HOME-04)</name>
  <files>sections/featured-products.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\announcement-bar.liquid — schema pattern
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-04 and §Interactive States — exact card classes
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md — confirmed requirements
  </read_first>
  <action>
    Create sections/featured-products.liquid implementing the Best Sellers product grid.

    Outer section: class "bg-sand py-12 md:py-16".
    Container: class "max-w-screen-xl mx-auto px-4".
    Heading h2: class "font-display text-3xl md:text-4xl text-deep text-center mb-8 md:mb-12" — outputs section.settings.heading (default: "Best Sellers").

    Conditional rendering: if section.settings.collection != blank, show the grid. Else show a centered placeholder paragraph in text-mid "Configure a collection in theme editor settings."

    Grid div: class "grid grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6".

    Iterate: {% assign featured_products = section.settings.collection.products | limit: section.settings.product_count %}
    {% for product in featured_products %}

    Each card: article element with class "relative group overflow-hidden bg-cream shadow-none group-hover:shadow-md transition-shadow duration-200".

    Image wrapper: div class "relative overflow-hidden aspect-[3/4]".
    Image: {{ product.featured_image | image_url: width: 600 | image_tag: class: 'w-full h-full object-cover scale-100 group-hover:scale-105 transition-transform duration-500', loading: 'lazy', alt: product.title }}.
    If product.featured_image is blank, render a div class "w-full h-full bg-sand" as placeholder.

    Quick-add form (inside image wrapper, absolutely positioned):
    form action="/cart/add" method="post" class "absolute bottom-0 left-0 right-0".
    Hidden input: name="id" value="{{ product.first_available_variant.id }}".
    Submit button: class "w-full bg-deep text-cream py-3 font-body text-xs tracking-widest uppercase translate-y-full group-hover:translate-y-0 transition-transform duration-200 cursor-pointer" — label "Quick Add".

    Card body: div class "p-4".
    Title: p class "font-body text-base font-semibold tracking-wide uppercase text-deep" — {{ product.title }}.
    Price: p class "font-body text-sm font-normal text-mid mt-1" — {{ product.price | money }}.

    {% endfor %}

    Schema settings:
    - id: "heading", type: "text", label: "Section heading", default: "Best Sellers"
    - id: "collection", type: "collection", label: "Collection"
    - id: "product_count", type: "range", label: "Number of products", min: 4, max: 8, step: 4, default: 4

    Schema name: "Featured Products", presets: [{ "name": "Featured Products" }].
  </action>
  <verify>
    <automated>grep -c "collection.products" F:\CODING\BikinniSite\sections\featured-products.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/featured-products.liquid exists
    - Contains {% schema %} and {% endschema %}
    - Schema name is "Featured Products"
    - Schema has setting with id "collection" and type "collection"
    - Schema has setting with id "product_count" and type "range"
    - Markup iterates collection.products with limit: section.settings.product_count
    - Markup contains "aspect-[3/4]" on the image wrapper
    - Markup contains "group-hover:scale-105 transition-transform duration-500" on the image
    - Markup contains "translate-y-full group-hover:translate-y-0 transition-transform duration-200" on the quick-add button
    - Quick-add form has action="/cart/add" method="post"
    - Hidden input name="id" references product.first_available_variant.id
    - Button label is "Quick Add"
    - Markup uses "bg-sand" on outer section
    - Grid uses "grid grid-cols-2 lg:grid-cols-4 gap-4 md:gap-6"
    - Heading uses "font-display text-3xl md:text-4xl text-deep"
    - Heading default is "Best Sellers"
  </acceptance_criteria>
  <done>Featured products section renders a 2-to-4 column grid from a configurable collection with quick-add slide-up on hover.</done>
</task>

<task type="auto">
  <name>Task 2: Build New Arrivals section (HOME-08)</name>
  <files>sections/new-arrivals.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\featured-products.liquid — reuse the same product card pattern just created
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-08 — view-all CTA spec
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md — discretion note on source (collection or newest)
  </read_first>
  <action>
    Create sections/new-arrivals.liquid. This section is structurally identical to featured-products.liquid with three differences: (1) background is bg-cream instead of bg-sand, (2) heading default is "New Arrivals", (3) a "View All New Arrivals →" CTA link appears below the grid.

    Outer section: class "bg-cream py-12 md:py-16".
    Container: class "max-w-screen-xl mx-auto px-4".
    Heading h2: class "font-display text-3xl md:text-4xl text-deep text-center mb-8 md:mb-12" — outputs section.settings.heading (default: "New Arrivals").

    Same grid structure and product card pattern as featured-products.liquid (copy and adapt). Use {% assign new_products = section.settings.collection.products | limit: section.settings.product_count %}.

    After the grid, CTA div: class "mt-8 text-center".
    CTA link a: href="{{ section.settings.collection.url }}" class "font-body text-sm font-semibold tracking-widest uppercase text-coral hover:text-deep transition-colors" — outputs section.settings.view_all_label (default: "View All New Arrivals →"). Conditional: only render the CTA if section.settings.collection != blank.

    Schema settings:
    - id: "heading", type: "text", label: "Section heading", default: "New Arrivals"
    - id: "collection", type: "collection", label: "Collection"
    - id: "product_count", type: "range", label: "Number of products", min: 4, max: 8, step: 4, default: 4
    - id: "view_all_label", type: "text", label: "View all link label", default: "View All New Arrivals →"

    Schema name: "New Arrivals", presets: [{ "name": "New Arrivals" }].
  </action>
  <verify>
    <automated>grep -c "view_all_label" F:\CODING\BikinniSite\sections\new-arrivals.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/new-arrivals.liquid exists
    - Contains {% schema %} and {% endschema %}
    - Schema name is "New Arrivals"
    - Schema has setting with id "collection" and type "collection"
    - Schema has setting with id "product_count" and type "range"
    - Schema has setting with id "view_all_label" with default "View All New Arrivals →"
    - Markup iterates collection.products with limit: section.settings.product_count
    - Markup contains "aspect-[3/4]" on the image wrapper
    - Quick-add form has action="/cart/add" with hidden input name="id"
    - Markup uses "bg-cream" on the outer section (NOT bg-sand)
    - CTA link uses "text-coral hover:text-deep transition-colors"
    - CTA link href references section.settings.collection.url
    - Heading default is "New Arrivals"
  </acceptance_criteria>
  <done>New arrivals section renders the same product card grid as featured products but with cream background, "New Arrivals" heading, and a view-all CTA linking to the collection URL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme editor → collection setting | Owner selects a collection; products are fetched server-side by Shopify |
| /cart/add form | Product variant ID submitted to Shopify cart API |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-07 | Tampering | /cart/add form variant ID | accept | Shopify validates variant ID server-side; invalid IDs return 422; no custom payment capture |
| T-03-08 | Injection | product.title output | mitigate | Liquid auto-escapes object properties in text contexts |
| T-03-09 | Tampering | npm/pip/cargo installs | accept | No package installs; pure Liquid files |
</threat_model>

<verification>
After both tasks complete:
- Both section files exist with collection-type schema settings
- Product cards render with correct aspect ratio, quick-add overlay, and hover scale
- View all CTA present in new-arrivals.liquid only
</verification>

<success_criteria>
- sections/featured-products.liquid and sections/new-arrivals.liquid both exist with {% schema %} blocks
- Both have collection picker schema settings and product_count range sliders
- Quick-add slide-up pattern consistent between both sections
- New arrivals includes view_all_label setting and rendered CTA link
</success_criteria>

<output>
Create `.planning/phases/03-home-page/03-03-SUMMARY.md` when done
</output>
