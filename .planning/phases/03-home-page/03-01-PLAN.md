---
phase: 03-home-page
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/hero.liquid
  - sections/ticker.liquid
autonomous: true
requirements:
  - HOME-01
  - HOME-02

must_haves:
  truths:
    - "Hero section renders a split layout: image left, dark text panel right"
    - "Hero image, headline, subheadline, CTA label, and CTA URL are all editable in theme editor"
    - "Hero left panel falls back to bg-sand when no image is uploaded"
    - "Ticker section displays scrolling brand promise text on a dark background"
    - "Ticker animation pauses on mouse hover"
  artifacts:
    - path: "sections/hero.liquid"
      provides: "Split-layout hero section with schema"
      contains: "{% schema %}"
    - path: "sections/ticker.liquid"
      provides: "CSS marquee ticker section with schema"
      contains: "{% schema %}"
  key_links:
    - from: "sections/hero.liquid"
      to: "templates/index.json"
      via: "section registration (wired in Plan 05)"
    - from: "sections/ticker.liquid"
      to: "templates/index.json"
      via: "section registration (wired in Plan 05)"
---

<objective>
Build the Hero section (HOME-01) and Ticker/Marquee section (HOME-02) as standalone Shopify Liquid section files.

Purpose: These are the first two visible elements of the home page — the hero establishes brand identity immediately, the ticker reinforces value props below the fold.
Output: sections/hero.liquid, sections/ticker.liquid
</objective>

<execution_context>
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\ROADMAP.md
@F:\CODING\BikinniSite\sections\announcement-bar.liquid
@F:\CODING\BikinniSite\sections\header.liquid

<interfaces>
<!-- Design tokens from UI-SPEC.md -->
<!-- bg-deep = var(--deep) #0d0a08 | bg-sand = var(--sand) #f5ede0 | bg-coral = var(--coral) #e85d3a -->
<!-- bg-cream = var(--cream) #faf6f0 | text-cream | text-coral | text-gold | text-mid -->
<!-- font-display = Cormorant Garamond | font-body = Barlow Condensed -->

<!-- Hero layout (D-01 to D-04, UI-SPEC §HOME-01): -->
<!-- - Desktop: flex row, image 55% left, text panel 45% right with bg-deep -->
<!-- - Mobile: flex col, image top (aspect-[4/3]), text below -->
<!-- - Text panel padding: px-8 py-16 md:px-12 md:py-20 lg:px-16 -->
<!-- - Headline: font-display text-4xl md:text-5xl lg:text-6xl font-normal leading-tight text-cream -->
<!-- - Subheadline: font-body text-lg md:text-xl font-normal tracking-wide text-cream -->
<!-- - CTA: bg-coral text-cream font-body text-sm font-semibold tracking-widest uppercase px-6 py-3 hover:bg-coral/90 transition-colors duration-200 focus:ring-2 focus:ring-coral focus:ring-offset-2 focus:outline-none -->
<!-- - Image fallback: left panel bg-sand if no image set -->
<!-- - Image render: {{ section.settings.image | image_url: width: 1200 | image_tag: class: 'w-full h-full object-cover', loading: 'eager' }} -->

<!-- Ticker (UI-SPEC §HOME-02): -->
<!-- - bg-deep py-3, text-cream ticker text, text-gold separator -->
<!-- - CSS @keyframes marquee animation: marquee 30s linear infinite -->
<!-- - Pause on hover: hover class sets animation-play-state: paused -->
<!-- - Default copy: "Dare to Bare · Inclusive Sizing XXS–3XL · New Drops Weekly · US-Based · Free Shipping Over $75" -->
<!-- - Text style: font-body text-sm font-semibold tracking-widest uppercase -->
<!-- - Separator: " · " wrapped in span.text-gold -->

<!-- Schema image_url pattern from header.liquid: -->
<!-- {{ section.settings.logo | image_url: width: 200 }} -->

<!-- Schema type: "image_picker" for images, "text" for copy, "url" for links -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Hero section (HOME-01)</name>
  <files>sections/hero.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\announcement-bar.liquid — schema structure pattern
    - F:\CODING\BikinniSite\sections\header.liquid — image_url + image_tag usage
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-01 — exact classes
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md — D-01 through D-04
  </read_first>
  <action>
    Create sections/hero.liquid implementing the split-layout hero per D-01–D-04 and UI-SPEC §HOME-01.

    Structure: outer div with class "flex flex-col md:flex-row min-h-[60vh] md:min-h-[80vh]".

    Left panel (image side, 55% desktop): "relative w-full md:w-[55%] aspect-[4/3] md:aspect-auto".
    - If section.settings.image is set: render image_tag with class "w-full h-full object-cover".
    - Else: apply class "bg-sand" to the panel div (graceful fallback per D-01).

    Right panel (text side, 45% desktop): "flex flex-col justify-center bg-deep px-8 py-16 md:px-12 md:py-20 lg:px-16 w-full md:w-[45%]".
    - h1: "font-display text-4xl md:text-5xl lg:text-6xl font-normal leading-tight text-cream" — outputs section.settings.heading (default: "[PLACEHOLDER — owner provides]")
    - p: "font-body text-lg md:text-xl font-normal tracking-wide text-cream mt-4" — outputs section.settings.subheading
    - a: href="{{ section.settings.cta_url }}" class "inline-block mt-8 bg-coral text-cream font-body text-sm font-semibold tracking-widest uppercase px-6 py-3 hover:bg-coral/90 transition-colors duration-200 focus:ring-2 focus:ring-coral focus:ring-offset-2 focus:outline-none active:bg-coral/80 active:scale-[0.98] transition-transform duration-100" — outputs section.settings.cta_label

    Schema settings (per D-04):
    - id: "image", type: "image_picker", label: "Hero image"
    - id: "heading", type: "text", label: "Headline", default: "[PLACEHOLDER — owner provides]"
    - id: "subheading", type: "text", label: "Subheadline", default: "[PLACEHOLDER — owner provides]"
    - id: "cta_label", type: "text", label: "CTA button label", default: "Shop Now"
    - id: "cta_url", type: "url", label: "CTA button URL"

    Schema name: "Hero", presets: [{ "name": "Hero" }].
  </action>
  <verify>
    <automated>grep -l "{% schema %}" F:\CODING\BikinniSite\sections\hero.liquid && grep -c "image_picker" F:\CODING\BikinniSite\sections\hero.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/hero.liquid exists
    - Contains {% schema %} and {% endschema %}
    - Schema has settings with ids: image, heading, subheading, cta_label, cta_url
    - image setting type is "image_picker"
    - cta_url setting type is "url"
    - Markup contains "bg-deep" on the right panel
    - Markup contains "bg-sand" as fallback class (in the else branch for missing image)
    - Markup contains "bg-coral text-cream" on the CTA anchor
    - Markup contains "font-display" on the h1
    - Markup contains "font-body" on the subheadline and CTA
    - Schema name is "Hero"
  </acceptance_criteria>
  <done>Hero section file exists with correct split layout, image fallback, owner-editable schema settings, and coral CTA button per D-01–D-04.</done>
</task>

<task type="auto">
  <name>Task 2: Build Ticker/Marquee section (HOME-02)</name>
  <files>sections/ticker.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\announcement-bar.liquid — existing marquee CSS pattern to extend
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-02 — exact animation spec
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md §Specific Ideas — marquee reuse note
  </read_first>
  <action>
    Create sections/ticker.liquid with a CSS marquee animation. The announcement-bar.liquid does NOT currently have a marquee animation — it uses static centered text. This section adds the scrolling marquee.

    Structure:
    - Outer div: "bg-deep py-3 overflow-hidden" with onmouseenter/onmouseleave to toggle animation-play-state (or use CSS :hover on the inner track).
    - Inner track div: id="ticker-track" class "flex whitespace-nowrap" with inline style "animation: marquee 30s linear infinite".
    - On hover of the outer section, set animation-play-state to paused. Use a CSS :hover rule in a <style> block: "#ticker-section:hover #ticker-track { animation-play-state: paused; }". Add id="ticker-section" to the outer div.
    - The track contains TWO copies of the content span (for seamless looping).
    - Content span: class "font-body text-sm font-semibold tracking-widest uppercase text-cream px-8" — outputs "Dare to Bare" + separator + "Inclusive Sizing XXS–3XL" + separator + "New Drops Weekly" + separator + "US-Based" + separator + "Free Shipping Over $75". Use section.settings.ticker_text if set, else use the default copy.
    - Separator span: class "text-gold mx-3" — outputs " · " between each phrase.
    - The @keyframes marquee animation in the <style> block: from { transform: translateX(0) } to { transform: translateX(-50%) }. Use -50% because the track has content duplicated (2x), so -50% of total width = one full loop.

    Style block at top of file (before the HTML):
    ```
    <style>
      @keyframes marquee {
        from { transform: translateX(0); }
        to { transform: translateX(-50%); }
      }
      #ticker-section:hover #ticker-track {
        animation-play-state: paused;
      }
    </style>
    ```

    Schema settings:
    - id: "ticker_text", type: "text", label: "Ticker text", info: "Leave blank to use default brand promises."

    Schema name: "Ticker / Marquee", presets: [{ "name": "Ticker / Marquee" }].

    Note: Do NOT include fenced code blocks in the action. The style tag above is directive prose describing the keyframes — implement it literally as a <style> tag in the liquid file.
  </action>
  <verify>
    <automated>grep -c "marquee" F:\CODING\BikinniSite\sections\ticker.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/ticker.liquid exists
    - Contains a &lt;style&gt; block with @keyframes marquee
    - Keyframe animates translateX(0) to translateX(-50%)
    - Contains id="ticker-section" and id="ticker-track"
    - ticker-track has animation: marquee applied (inline style or CSS class)
    - CSS rule "#ticker-section:hover #ticker-track" sets animation-play-state: paused
    - Content contains "Dare to Bare" as default text (either hardcoded in the else branch or as schema default)
    - Text uses class "text-gold" for separator · characters
    - Text uses classes "font-body text-sm font-semibold tracking-widest uppercase text-cream"
    - Schema name is "Ticker / Marquee"
    - Contains {% schema %} and {% endschema %}
  </acceptance_criteria>
  <done>Ticker section exists with CSS marquee animation, pause-on-hover behavior, gold separators, and owner-editable text via theme editor.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme editor → section settings | Owner-provided text and image URLs injected into Liquid output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Injection | section.settings.heading output | mitigate | Shopify Liquid auto-escapes text settings; no raw filter used |
| T-03-02 | Injection | section.settings.cta_url | mitigate | Use type: "url" schema setting — Shopify validates and sanitizes URL values |
| T-03-03 | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; pure Liquid files |
</threat_model>

<verification>
After both tasks complete:
- shopify theme dev (or push to staging) renders the home page
- Hero section shows correct split layout in browser at 375px and 1280px
- Ticker scrolls continuously and pauses on hover
</verification>

<success_criteria>
- sections/hero.liquid and sections/ticker.liquid both exist with {% schema %} blocks
- Hero schema has 5 settings: image (image_picker), heading (text), subheading (text), cta_label (text), cta_url (url)
- Ticker CSS @keyframes marquee present; hover pause CSS rule present
- Both sections will be wired into templates/index.json in Plan 05
</success_criteria>

<output>
Create `.planning/phases/03-home-page/03-01-SUMMARY.md` when done
</output>
