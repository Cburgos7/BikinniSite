---
phase: 03-home-page
plan: 06
type: execute
wave: 4
depends_on:
  - 03-01
  - 03-02
  - 03-03
  - 03-04
  - 03-05
files_modified:
  - templates/index.json
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - HOME-06
  - HOME-07
  - HOME-08
  - HOME-09

must_haves:
  truths:
    - "templates/index.json order array contains all 9 section keys in the correct order (D-05)"
    - "templates/index.json sections map contains a valid entry for each of the 9 sections"
    - "Home page renders all 9 sections when viewed in a browser at 375px, 768px, and 1280px"
    - "Each section is accessible and editable in the Shopify theme editor"
  artifacts:
    - path: "templates/index.json"
      provides: "Home page template wiring all 9 sections"
      contains: "\"order\":"
  key_links:
    - from: "templates/index.json order array"
      to: "sections/*.liquid"
      via: "Shopify section rendering via {{ content_for_layout }}"
---

<objective>
Wire all 9 home page sections into templates/index.json in the correct order (D-05) and perform a final responsive QA verification.

Purpose: All 9 sections were built in Waves 1–3. This final plan registers them in the home page template so Shopify renders them in the correct order. No new Liquid is written — this is configuration-only.
Output: templates/index.json (fully populated)
</objective>

<execution_context>
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\ROADMAP.md
@F:\CODING\BikinniSite\layout\theme.liquid

<interfaces>
<!-- templates/index.json current state (read at task start): -->
<!-- { "sections": {}, "order": [] } -->

<!-- Shopify JSON template format: -->
<!-- { -->
<!--   "sections": { -->
<!--     "key": { "type": "section-file-name", "settings": {}, "blocks": {} } -->
<!--   }, -->
<!--   "order": ["key1", "key2", ...] -->
<!-- } -->

<!-- Section file-to-type mapping (type = filename without .liquid): -->
<!-- sections/hero.liquid          → type: "hero" -->
<!-- sections/ticker.liquid        → type: "ticker" -->
<!-- sections/category-grid.liquid → type: "category-grid" -->
<!-- sections/featured-products.liquid → type: "featured-products" -->
<!-- sections/social-feed.liquid   → type: "social-feed" -->
<!-- sections/brand-promise.liquid → type: "brand-promise" -->
<!-- sections/testimonials.liquid  → type: "testimonials" -->
<!-- sections/new-arrivals.liquid  → type: "new-arrivals" -->
<!-- sections/sizing-banner.liquid → type: "sizing-banner" -->

<!-- Correct order per D-05: -->
<!-- 1. hero, 2. ticker, 3. category-grid, 4. featured-products, 5. social-feed, -->
<!-- 6. brand-promise, 7. testimonials, 8. new-arrivals, 9. sizing-banner -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire all 9 sections into templates/index.json</name>
  <files>templates/index.json</files>
  <read_first>
    - F:\CODING\BikinniSite\templates\index.json — current state (currently empty: {"sections": {}, "order": []})
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md — D-05 section order
    - F:\CODING\BikinniSite\layout\theme.liquid — confirm {{ content_for_layout }} renders sections
  </read_first>
  <action>
    Overwrite templates/index.json with a complete JSON template that registers all 9 home page sections in the D-05 order.

    The sections map must include one entry per section. Use simple string keys matching the section type (e.g., key "hero" with type "hero"). The settings object for each section should be empty ({}) — the Shopify theme editor will populate settings. Sections with blocks (brand-promise, testimonials, category-grid) need an empty "blocks" object ({}) and "block_order" array ([]) in their entry so Shopify can track editor-added blocks.

    Exact section key and type pairs (key = type in this case for clarity):
    - "hero" → type "hero"
    - "ticker" → type "ticker"
    - "category-grid" → type "category-grid"
    - "featured-products" → type "featured-products"
    - "social-feed" → type "social-feed"
    - "brand-promise" → type "brand-promise"
    - "testimonials" → type "testimonials"
    - "new-arrivals" → type "new-arrivals"
    - "sizing-banner" → type "sizing-banner"

    The "order" array must be exactly: ["hero","ticker","category-grid","featured-products","social-feed","brand-promise","testimonials","new-arrivals","sizing-banner"]

    The root-level JSON must also include "name": "Default" (Shopify convention for the default home template).

    Validate the output is valid JSON before writing (no trailing commas, no comments).
  </action>
  <verify>
    <automated>node -e "const f=require('fs');const j=JSON.parse(f.readFileSync('F:/CODING/BikinniSite/templates/index.json','utf8'));console.log(j.order.length === 9 ? 'PASS: 9 sections' : 'FAIL: ' + j.order.length + ' sections')"</automated>
  </verify>
  <acceptance_criteria>
    - templates/index.json is valid JSON (node -e JSON.parse confirms no syntax error)
    - j.order.length === 9
    - j.order[0] === "hero"
    - j.order[1] === "ticker"
    - j.order[2] === "category-grid"
    - j.order[3] === "featured-products"
    - j.order[4] === "social-feed"
    - j.order[5] === "brand-promise"
    - j.order[6] === "testimonials"
    - j.order[7] === "new-arrivals"
    - j.order[8] === "sizing-banner"
    - j.sections contains keys for all 9 section names
    - j.sections["hero"].type === "hero"
    - j.sections["brand-promise"].blocks is an empty object {}
    - j.sections["testimonials"].blocks is an empty object {}
    - j.sections["category-grid"].blocks is an empty object {}
    - File passes JSON.parse with no error
  </acceptance_criteria>
  <done>templates/index.json fully populated with all 9 sections in D-05 order. Home page will render all sections in the correct sequence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    All 9 home page sections built (Plans 01–05) and wired into templates/index.json (Plan 06 Task 1). The complete home page should now render in development.
  </what-built>
  <how-to-verify>
    1. Run `shopify theme dev` (or push to your staging branch and open the Shopify preview URL).
    2. Open the home page in a browser.
    3. Confirm all 9 sections appear in this order: Hero → Ticker → Category Grid → Featured Products → Social Feed Strip → Brand Promise Strip → Testimonials → New Arrivals → Sizing Banner.
    4. Resize to 375px (mobile): confirm hero stacks vertically (image top, text below), grids collapse to 2 columns, social feed scroll is horizontal.
    5. Resize to 768px (tablet): confirm grids transition correctly.
    6. Resize to 1280px (desktop): confirm hero split layout, 4-column grids, all text visible.
    7. Open Shopify theme editor (Customize): confirm all 9 sections are listed in the sidebar and their settings are editable.
    8. Hover the ticker → confirm it pauses.
    9. If social_post metaobjects exist: confirm social feed shows cards. If not, confirm the empty-state message appears.
    10. If testimonial blocks have been added in the editor: confirm the carousel auto-rotates after 5 seconds and pauses on hover.
  </how-to-verify>
  <resume-signal>Type "approved" to confirm all sections render correctly, or describe any issues found.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/index.json → Shopify rendering | Template file is version-controlled; Shopify reads it server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-18 | Tampering | templates/index.json section type values | accept | File is git-controlled; Shopify validates section type against files in sections/ directory |
| T-03-19 | Tampering | npm/pip/cargo installs | accept | No package installs; JSON config file only |
</threat_model>

<verification>
- node -e JSON.parse on templates/index.json returns no error
- j.order.length === 9
- All 9 section types correspond to existing .liquid files in sections/
- Human checkpoint confirms visual render and theme editor editability
</verification>

<success_criteria>
1. All 9 content sections render on the home page in D-05 order
2. Featured products section pulls live products from the owner-configured collection
3. Social feed displays social_post metaobject cards (or empty-state message if none configured)
4. Every section is editable in Shopify theme editor without touching code
5. Home page passes responsive checks at 375px, 768px, and 1280px
</success_criteria>

<output>
Create `.planning/phases/03-home-page/03-06-SUMMARY.md` when done
</output>
