---
phase: 03-home-page
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/brand-promise.liquid
  - sections/sizing-banner.liquid
autonomous: true
requirements:
  - HOME-06
  - HOME-09

must_haves:
  truths:
    - "Brand promise strip renders 4 promise cards on a dark background"
    - "Each promise card has an inline SVG icon, heading, and descriptor"
    - "Brand promise cards are owner-editable via theme editor blocks"
    - "Sizing banner renders size pills XXS through 3X on a dark background"
    - "Sizing banner links to /pages/size-guide"
    - "Size pills have correct hover state (border-coral text-coral)"
  artifacts:
    - path: "sections/brand-promise.liquid"
      provides: "4-column brand promise strip with block schema"
      contains: "{% schema %}"
    - path: "sections/sizing-banner.liquid"
      provides: "Sizing banner with size pills and size guide CTA"
      contains: "{% schema %}"
  key_links:
    - from: "sections/brand-promise.liquid"
      to: "templates/index.json"
      via: "section registration (wired in Plan 05)"
    - from: "sections/sizing-banner.liquid"
      to: "templates/index.json"
      via: "section registration (wired in Plan 05)"
---

<objective>
Build the Brand Promise strip (HOME-06) and Sizing Banner (HOME-09) as standalone Shopify Liquid section files.

Purpose: Both are static dark-background sections requiring no JS or dynamic data — they establish brand credibility and route customers to the size guide.
Output: sections/brand-promise.liquid, sections/sizing-banner.liquid
</objective>

<execution_context>
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\ROADMAP.md
@F:\CODING\BikinniSite\sections\announcement-bar.liquid

<interfaces>
<!-- Design tokens: bg-deep, bg-cream, bg-sand, bg-coral, text-cream, text-gold, text-mid, text-coral -->
<!-- font-display = Cormorant Garamond | font-body = Barlow Condensed -->

<!-- Brand Promise (UI-SPEC §HOME-06): -->
<!-- bg-deep py-10 md:py-12 -->
<!-- Grid: grid grid-cols-2 md:grid-cols-4 gap-8, items centered (text-center) -->
<!-- Each promise: inline SVG (24x24 text-gold) + heading (font-body text-sm font-semibold tracking-widest uppercase text-cream) + descriptor (font-body text-xs text-mid) -->
<!-- 4 default promises: "Premium Fabrics" / "Sizes XXS–3XL" / "New Drops Weekly" / "US-Based & Proud" -->
<!-- Blocks schema: each block has fields: icon_choice (select), heading (text), descriptor (text) -->

<!-- Sizing Banner (UI-SPEC §HOME-09): -->
<!-- bg-deep py-10 md:py-12 -->
<!-- Layout: text-center, max-w-2xl mx-auto -->
<!-- Heading: font-display text-2xl md:text-3xl text-cream — "Find Your Perfect Fit" -->
<!-- Size order: XXS · XS · S · M · L · XL · 1X · 2X · 3X -->
<!-- Pill: span — border border-cream/30 px-3 py-1 font-body text-xs tracking-wide text-cream/80 hover:border-coral hover:text-coral transition-colors cursor-pointer -->
<!-- Pills wrap: flex flex-wrap justify-center gap-2 mt-6 -->
<!-- CTA: "View Full Size Guide →" href="/pages/size-guide" — text-coral hover:text-gold transition-colors font-body text-sm font-semibold tracking-widest uppercase mt-6 inline-block -->
<!-- Banner heading is owner-editable; CTA label and URL are owner-editable -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build Brand Promise strip section (HOME-06)</name>
  <files>sections/brand-promise.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\announcement-bar.liquid — schema block pattern reference
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-06 — exact classes and promise copy
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md §HOME-06 — confirmed requirements
  </read_first>
  <action>
    Create sections/brand-promise.liquid as a dark-panel, 4-column promise grid.

    Outer section: class "bg-deep py-10 md:py-12".
    Inner container: class "max-w-screen-xl mx-auto px-4".
    Grid: class "grid grid-cols-2 md:grid-cols-4 gap-8".

    Iterate blocks with "{% for block in section.blocks %}" and output each promise as a centered div (class "flex flex-col items-center text-center gap-3 {{ block.shopify_attributes }}"):
    - SVG icon: inline 24x24 SVG. Use a simple placeholder SVG star/diamond shape in text-gold for all icons (since this is Liquid, not a design system — owner can request icon variants later). Render the icon unconditionally; keep all 4 identical placeholder SVGs in the default preset.
    - Heading: class "font-body text-sm font-semibold tracking-widest uppercase text-cream" — outputs block.settings.heading
    - Descriptor: class "font-body text-xs text-mid" — outputs block.settings.descriptor

    Schema:
    - name: "Brand Promise Strip"
    - blocks: type "promise" with settings:
      - id: "heading", type: "text", label: "Promise heading"
      - id: "descriptor", type: "text", label: "Short descriptor"
    - max_blocks: 4
    - presets: one preset named "Brand Promise Strip" with 4 blocks:
      Block 1: heading "Premium Fabrics", descriptor "Carefully sourced materials"
      Block 2: heading "Sizes XXS–3XL", descriptor "Inclusive fit for every body"
      Block 3: heading "New Drops Weekly", descriptor "Always something fresh"
      Block 4: heading "US-Based & Proud", descriptor "Shipped from the US"

    Note: The icon is a simple decorative diamond SVG in each block (hardcoded, not a schema field) — all four blocks share the same inline SVG since the brand promise strip icons are decorative, not data-driven.
  </action>
  <verify>
    <automated>grep -c "{% schema %}" F:\CODING\BikinniSite\sections\brand-promise.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/brand-promise.liquid exists
    - Contains {% schema %} and {% endschema %}
    - Schema name is "Brand Promise Strip"
    - Schema has a blocks definition with type "promise"
    - Block settings include heading and descriptor (both type "text")
    - max_blocks is 4
    - Preset defines 4 blocks with headings: "Premium Fabrics", "Sizes XXS–3XL", "New Drops Weekly", "US-Based & Proud"
    - Markup iterates section.blocks with {% for block in section.blocks %}
    - Markup uses class "grid grid-cols-2 md:grid-cols-4"
    - Markup uses "bg-deep" on the outer section
    - Markup uses "text-gold" on the SVG icon element
    - Markup uses "font-body text-sm font-semibold tracking-widest uppercase text-cream" on the heading
    - Markup uses "font-body text-xs text-mid" on the descriptor
  </acceptance_criteria>
  <done>Brand promise strip section exists with 4-column grid, block-based editing, gold SVG icons, and correct dark-panel styling.</done>
</task>

<task type="auto">
  <name>Task 2: Build Sizing Banner section (HOME-09)</name>
  <files>sections/sizing-banner.liquid</files>
  <read_first>
    - F:\CODING\BikinniSite\sections\announcement-bar.liquid — schema structure pattern
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-UI-SPEC.md §HOME-09 — pill styles, CTA, heading
    - F:\CODING\BikinniSite\.planning\phases\03-home-page\03-CONTEXT.md §Specific Ideas — sizing pill CSS note
  </read_first>
  <action>
    Create sections/sizing-banner.liquid as a centered dark-panel sizing showcase.

    Outer section: class "bg-deep py-10 md:py-12".
    Inner container: class "max-w-2xl mx-auto px-4 text-center".

    Elements:
    - Heading h2: class "font-display text-2xl md:text-3xl text-cream" — outputs section.settings.heading (default: "Find Your Perfect Fit")
    - Pill row div: class "flex flex-wrap justify-center gap-2 mt-6"
    - Pills: render a span for each size in the fixed order XXS, XS, S, M, L, XL, 1X, 2X, 3X. Each span class "border border-cream/30 px-3 py-1 font-body text-xs tracking-wide text-cream/80 hover:border-coral hover:text-coral transition-colors cursor-pointer". Use an assign array trick: {% assign sizes = "XXS,XS,S,M,L,XL,1X,2X,3X" | split: "," %} then {% for size in sizes %} ... {% endfor %}.
    - CTA link a: class "mt-6 inline-block text-coral hover:text-gold transition-colors font-body text-sm font-semibold tracking-widest uppercase" href="{{ section.settings.cta_url | default: '/pages/size-guide' }}" — outputs section.settings.cta_label (default: "View Full Size Guide →")

    Schema settings:
    - id: "heading", type: "text", label: "Banner heading", default: "Find Your Perfect Fit"
    - id: "cta_label", type: "text", label: "CTA label", default: "View Full Size Guide →"
    - id: "cta_url", type: "url", label: "CTA URL"

    Schema name: "Sizing Banner", presets: [{ "name": "Sizing Banner" }].
  </action>
  <verify>
    <automated>grep -c "{% schema %}" F:\CODING\BikinniSite\sections\sizing-banner.liquid</automated>
  </verify>
  <acceptance_criteria>
    - sections/sizing-banner.liquid exists
    - Contains {% schema %} and {% endschema %}
    - Schema name is "Sizing Banner"
    - Schema settings include ids: heading, cta_label, cta_url
    - cta_url type is "url"
    - Markup contains {% assign sizes = "XXS,XS,S,M,L,XL,1X,2X,3X" | split: "," %} or equivalent size iteration
    - Markup outputs span elements with class "border border-cream/30" for size pills
    - Markup includes hover classes "hover:border-coral hover:text-coral"
    - Markup uses "bg-deep" on the outer section
    - Markup uses "font-display" on the heading
    - CTA link uses "text-coral hover:text-gold"
    - Heading default is "Find Your Perfect Fit"
    - CTA label default is "View Full Size Guide →"
  </acceptance_criteria>
  <done>Sizing banner section exists with correct size pill row, dark-panel styling, hover states, and owner-editable heading and CTA.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme editor → section settings | Owner-provided heading and CTA URL injected into Liquid output |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Injection | section.settings.heading output | mitigate | Shopify Liquid auto-escapes text settings |
| T-03-05 | Injection | section.settings.cta_url | mitigate | Use type: "url" schema setting — Shopify validates URL values |
| T-03-06 | Tampering | npm/pip/cargo installs | accept | No package installs; pure Liquid files |
</threat_model>

<verification>
After both tasks complete:
- sections/brand-promise.liquid and sections/sizing-banner.liquid exist with valid {% schema %} blocks
- Both sections can be previewed in Shopify theme editor (after being wired in Plan 05)
</verification>

<success_criteria>
- sections/brand-promise.liquid has blocks schema with max_blocks: 4 and default preset with 4 promise blocks
- sections/sizing-banner.liquid renders size pills XXS through 3X with hover:border-coral hover:text-coral classes
- Both sections use bg-deep as the outer background
</success_criteria>

<output>
Create `.planning/phases/03-home-page/03-02-SUMMARY.md` when done
</output>
