---
phase: 05-content-pages
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/page-models.liquid
autonomous: true
requirements:
  - PAGE-02

must_haves:
  truths:
    - "Models page renders all model metaobject entries in a 3-column grid (up to 10)"
    - "Each model card shows portrait image with hover scale, name, height + size worn, bio, and optional Instagram link"
    - "Empty state renders 'New faces coming soon.' heading and descriptive body text"
    - "Section has {% schema %} block with heading and subtitle settings"
  artifacts:
    - path: sections/page-models.liquid
      provides: Models grid section iterating shop.metaobjects.model entries
      contains: "{% schema %}"
  key_links:
    - from: sections/page-models.liquid
      to: shop.metaobjects.model.values
      via: "{%- assign models = shop.metaobjects.model.values -%}"
      pattern: "shop.metaobjects.model"
---

<objective>
Build the Models page section — a grid of model portraits sourced from the `model` metaobject, matching D-01 and D-10 specs.

Purpose: Allow the brand to showcase models editing entries in Shopify admin, no code changes needed.
Output: `sections/page-models.liquid`
</objective>

<execution_context>
@F:/CODING/BikinniSite/.claude/get-shit-done/workflows/execute-plan.md
@F:/CODING/BikinniSite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CODING/BikinniSite/.planning/ROADMAP.md
@F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md

<interfaces>
<!-- Metaobject iteration pattern (from sections/social-feed.liquid): -->
<!--   {%- assign posts = shop.metaobjects.social_post.values -%} -->
<!--   {%- for post in posts -%} ... {%- endfor -%} -->
<!-- model metaobject fields (from Phase 2 META-02 definition): -->
<!--   .name (single_line_text), .portrait (file_reference / image), -->
<!--   .height (single_line_text), .size_worn (single_line_text), -->
<!--   .bio (multi_line_text), .instagram_handle (single_line_text) -->
<!-- D-01 grid: grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8 -->
<!-- D-10 hero: bg-deep text-cream py-16 lg:py-24 -->
<!-- Hard cap: limit: 10 on the for loop -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build page-models.liquid</name>
  <files>sections/page-models.liquid</files>
  <read_first>
    - F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md (D-01, D-10)
    - F:/CODING/BikinniSite/sections/social-feed.liquid (metaobject iteration pattern)
  </read_first>
  <action>
Create `sections/page-models.liquid` implementing D-01 and D-10.

Structure:

1. Page hero (D-10): `<section class="bg-deep text-cream py-16 lg:py-24">`. H1: `font-display text-4xl lg:text-6xl font-normal leading-tight text-center` — renders `{{ section.settings.heading | escape }}`, default "The Faces of Soleil Noir". Optional subtitle: `font-body text-xs uppercase tracking-widest text-cream/60 mt-4 text-center` — renders `{{ section.settings.subtitle | escape }}`, shown only when non-blank.

2. Grid section: `<section class="max-w-screen-xl mx-auto px-4 lg:px-8 py-12 lg:py-16">`.

   Assign: `{%- assign models = shop.metaobjects.model.values -%}`

   If `models.size > 0`:
   - Render grid: `<div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">`.
   - Loop `{%- for model in models limit: 10 -%}`. Each iteration renders one model card:

     Card wrapper: `<div class="bg-cream group">`.

     Portrait container: `<div class="aspect-[2/3] overflow-hidden">`. Inside: if `model.portrait != blank`, render via `{{ model.portrait | image_url: width: 600 | image_tag: class: 'object-cover w-full h-full scale-100 group-hover:scale-105 transition-transform duration-500', loading: 'lazy', alt: model.name }}`. If blank: `<div class="w-full h-full bg-sand"></div>`.

     Info below image: `<div class="p-4">`. Name: `<h2 class="font-display text-2xl font-normal text-deep">{{ model.name | escape }}</h2>`. Height + size worn on one line: `<p class="font-body text-xs uppercase tracking-widest text-mid mt-1">{{ model.height | escape }}{% if model.height != blank and model.size_worn != blank %} &mdash; {% endif %}{{ model.size_worn | escape }}</p>`. Bio: `<p class="font-body text-sm text-mid leading-relaxed mt-2 line-clamp-3">{{ model.bio | escape }}</p>`. Instagram (if present): `{%- if model.instagram_handle != blank -%}<a href="https://instagram.com/{{ model.instagram_handle | remove: '@' | escape }}" class="font-body text-xs uppercase tracking-widest text-coral hover:text-deep transition-colors mt-2 inline-block" rel="noopener noreferrer" target="_blank">@{{ model.instagram_handle | remove: '@' | escape }}</a>{%- endif -%}`.

   Else (empty state): `<div class="py-24 text-center"><h2 class="font-display text-3xl font-normal text-deep">New faces coming soon.</h2><p class="font-body text-sm text-mid leading-relaxed mt-4">Check back after our next drop.</p></div>`.

Schema settings: `heading` (text, default "The Faces of Soleil Noir"), `subtitle` (text, label "Hero subtitle (optional)"). Schema name: "Models Page". Add presets with one preset named "Models Page".

Security: all `{{ model.* }}` outputs use `| escape`. Instagram handle de-prefixed with `| remove: '@'` then `| escape` before interpolation into href and display. Image rendered via `image_tag` filter (Shopify-sanitized).
  </action>
  <verify>
    <automated>grep -c "{% schema %}\|shop.metaobjects.model\|grid-cols-1 sm:grid-cols-2 lg:grid-cols-3\|limit: 10" F:/CODING/BikinniSite/sections/page-models.liquid</automated>
  </verify>
  <acceptance_criteria>
    - File `sections/page-models.liquid` exists and is non-empty
    - `grep "{% schema %}" sections/page-models.liquid` returns a match
    - `grep "shop.metaobjects.model.values" sections/page-models.liquid` returns a match
    - `grep "limit: 10" sections/page-models.liquid` returns a match
    - `grep "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8" sections/page-models.liquid` returns a match
    - `grep "aspect-\[2/3\]" sections/page-models.liquid` returns a match
    - `grep "group-hover:scale-105" sections/page-models.liquid` returns a match
    - `grep "line-clamp-3" sections/page-models.liquid` returns a match
    - `grep "instagram" sections/page-models.liquid` returns a match
    - `grep "New faces coming soon" sections/page-models.liquid` returns a match
    - `grep "| escape" sections/page-models.liquid` returns at least 4 matches
  </acceptance_criteria>
  <done>page-models.liquid renders D-01 portrait grid with hover, bio, Instagram link, empty state, and D-10 hero; schema present with heading/subtitle settings</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify admin → metaobject | Model data entered by merchant in Shopify admin |
| Liquid → HTML output | Metaobject field values rendered into page HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | model.name / model.bio / model.height outputs | mitigate | All metaobject field values rendered with `| escape` |
| T-05-02-02 | Tampering | instagram_handle interpolated into href | mitigate | `| remove: '@' | escape` before href and display rendering |
| T-05-02-03 | Information Disclosure | External Instagram link | accept | Opens in new tab with `rel="noopener noreferrer"`; no PII exposed |
</threat_model>

<verification>
- `sections/page-models.liquid` exists, iterates `shop.metaobjects.model.values` with `limit: 10`
- D-01 grid classes, portrait aspect ratio, hover scale, and Instagram link all present
- Empty state copy matches copywriting contract
- All metaobject outputs escaped
</verification>

<success_criteria>
Models page renders up to 10 model cards in a responsive 3-column grid with portrait, bio, height, size worn, and Instagram link. Empty state displays when no metaobject entries exist.
</success_criteria>

<output>
Create `.planning/phases/05-content-pages/05-02-SUMMARY.md` when done.
</output>
