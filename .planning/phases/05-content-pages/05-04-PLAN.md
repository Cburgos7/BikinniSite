---
phase: 05-content-pages
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/page-affiliates.liquid
autonomous: true
requirements:
  - PAGE-05

must_haves:
  truths:
    - "Affiliates page renders D-10 hero with 'Partner With Us' H1 and subtitle"
    - "Three tier cards render in a 3-column grid: Standard (10%), Silver (12%), Gold (15%)"
    - "Gold tier card is inverted: bg-deep text-cream"
    - "Commission percentages display in coral (font-display text-5xl text-coral)"
    - "UpPromote iframe embeds when merchant sets upromote_url; fallback link button shown when blank"
    - "Section has {% schema %} with heading, subtitle, and upromote_url settings"
  artifacts:
    - path: sections/page-affiliates.liquid
      provides: Affiliates page with 3 tier cards + UpPromote iframe embed
      contains: "{% schema %}"
  key_links:
    - from: sections/page-affiliates.liquid
      to: section.settings.upromote_url
      via: iframe src and fallback link button
      pattern: "upromote_url"
---

<objective>
Build the Affiliates page section — three commission tier cards and the UpPromote registration iframe embed — per D-03 and D-10.

Purpose: Showcase affiliate program tiers and let creators sign up without leaving the site.
Output: `sections/page-affiliates.liquid`
</objective>

<execution_context>
@F:/CODING/BikinniSite/.claude/get-shit-done/workflows/execute-plan.md
@F:/CODING/BikinniSite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CODING/BikinniSite/.planning/ROADMAP.md
@F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md

<interfaces>
<!-- D-03 tier card specs: -->
<!-- Standard (10%): bg-sand border border-deep/10 p-6 -->
<!-- Silver (12%): bg-sand border border-deep/20 p-6 ring-1 ring-mid -->
<!-- Gold (15%): bg-deep text-cream p-6 (inverted) -->
<!-- Commission %: font-display text-5xl font-normal text-coral -->
<!-- Tier name: font-body text-xs uppercase tracking-widest text-mid mt-1 (Gold: text-cream/60) -->
<!-- Perks: mt-4 space-y-2 font-body text-sm text-mid (Gold: text-cream/80), "— " prefix -->
<!-- UpPromote iframe: class="w-full min-h-[600px] border-0 mt-12" loading="lazy" -->
<!-- Fallback: bg-deep text-cream button with UpPromote URL setting -->
<!-- D-10 hero: bg-deep text-cream py-16 lg:py-24 -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build page-affiliates.liquid</name>
  <files>sections/page-affiliates.liquid</files>
  <read_first>
    - F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md (D-03, D-10)
    - F:/CODING/BikinniSite/sections/hero.liquid (schema settings pattern)
  </read_first>
  <action>
Create `sections/page-affiliates.liquid` implementing D-03 and D-10.

Structure:

1. Page hero (D-10): `<section class="bg-deep text-cream py-16 lg:py-24">`. H1 `font-display text-4xl lg:text-6xl font-normal leading-tight text-center` renders `{{ section.settings.heading | escape }}`, default "Partner With Us". Subtitle `font-body text-xs uppercase tracking-widest text-cream/60 mt-4 text-center` renders `{{ section.settings.subtitle | escape }}`, default "Earn commissions sharing styles you love." Show subtitle only if non-blank.

2. Tier cards section: `<section class="max-w-screen-xl mx-auto px-4 lg:px-8 py-12 lg:py-16">`.

   Section label above cards: `<p class="font-body text-xs uppercase tracking-widest text-deep text-center mb-8">Join Our Creator Community</p>`.

   Grid: `<div class="grid grid-cols-1 md:grid-cols-3 gap-6 mt-8">`.

   Three hardcoded tier cards (content from copywriting contract + D-03):

   Card 1 — Standard (10%):
   ```
   <div class="bg-sand border border-deep/10 p-6">
     <p class="font-display text-5xl font-normal text-coral">10%</p>
     <p class="font-body text-xs uppercase tracking-widest text-mid mt-1">Standard</p>
     <ul class="mt-4 space-y-2 font-body text-sm text-mid">
       <li>— Commission on every sale</li>
       <li>— Unique referral link</li>
       <li>— Monthly payouts</li>
     </ul>
   </div>
   ```

   Card 2 — Silver (12%):
   ```
   <div class="bg-sand border border-deep/20 p-6 ring-1 ring-mid">
     <p class="font-display text-5xl font-normal text-coral">12%</p>
     <p class="font-body text-xs uppercase tracking-widest text-mid mt-1">Silver</p>
     <ul class="mt-4 space-y-2 font-body text-sm text-mid">
       <li>— Boosted commission rate</li>
       <li>— Priority support</li>
       <li>— Early access to drops</li>
     </ul>
   </div>
   ```

   Card 3 — Gold (15%) — inverted:
   ```
   <div class="bg-deep text-cream p-6">
     <p class="font-display text-5xl font-normal text-coral">15%</p>
     <p class="font-body text-xs uppercase tracking-widest text-cream/60 mt-1">Gold</p>
     <ul class="mt-4 space-y-2 font-body text-sm text-cream/80">
       <li>— Top-tier commission</li>
       <li>— Dedicated account manager</li>
       <li>— Co-branded content opportunities</li>
     </ul>
   </div>
   ```

3. UpPromote embed / fallback: below the grid, check `section.settings.upromote_url != blank`. If set: `<iframe src="{{ section.settings.upromote_url | escape }}" class="w-full min-h-[600px] border-0 mt-12" loading="lazy" title="Affiliate Registration"></iframe>`. If blank: `<div class="mt-12 text-center"><a href="{{ section.settings.upromote_url | default: '#' | escape }}" class="inline-block bg-deep text-cream font-body text-xs tracking-widest uppercase py-4 px-8 hover:opacity-90 transition-opacity">Apply Now</a></div>`. Use `section.settings.upromote_url | escape` on both iframe src and fallback href.

Schema settings: `heading` (text, default "Partner With Us"), `subtitle` (text, default "Earn commissions sharing styles you love."), `upromote_url` (text, label "UpPromote registration URL", info "Paste the UpPromote hosted registration page URL — it will embed as an iframe"). Schema name: "Affiliates Page". Presets: one preset named "Affiliates Page".

Security: `upromote_url` rendered with `| escape` on both iframe src and fallback href. Tier card content is hardcoded — no user input rendered.
  </action>
  <verify>
    <automated>grep -c "{% schema %}\|bg-deep text-cream p-6\|upromote_url\|text-coral" F:/CODING/BikinniSite/sections/page-affiliates.liquid</automated>
  </verify>
  <acceptance_criteria>
    - File `sections/page-affiliates.liquid` exists and is non-empty
    - `grep "{% schema %}" sections/page-affiliates.liquid` returns a match
    - `grep "bg-deep text-cream py-16 lg:py-24" sections/page-affiliates.liquid` returns a match
    - `grep "grid-cols-1 md:grid-cols-3 gap-6" sections/page-affiliates.liquid` returns a match
    - `grep "text-5xl font-normal text-coral" sections/page-affiliates.liquid` returns 3 matches (one per tier)
    - `grep "bg-deep text-cream p-6" sections/page-affiliates.liquid` returns a match (Gold inverted card)
    - `grep "upromote_url" sections/page-affiliates.liquid` returns at least 2 matches (schema + render)
    - `grep "min-h-\[600px\]" sections/page-affiliates.liquid` returns a match (iframe)
    - `grep "| escape" sections/page-affiliates.liquid` returns at least 3 matches
  </acceptance_criteria>
  <done>page-affiliates.liquid renders D-10 hero, 3-column tier card grid with correct D-03 styles, UpPromote iframe when URL is set, fallback apply button when URL is blank; all settings escaped</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merchant → Shopify admin | upromote_url setting entered in theme editor |
| Liquid → HTML output | URL value interpolated into iframe src and anchor href |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04-01 | Tampering | upromote_url in iframe src | mitigate | `| escape` filter applied; Shopify's text setting type strips dangerous protocols |
| T-05-04-02 | Tampering | upromote_url in fallback href | mitigate | `| escape` filter applied |
| T-05-04-03 | Information Disclosure | iframe sandboxing | accept | UpPromote is a merchant-controlled trusted third-party; no customer PII sent via iframe src param |
</threat_model>

<verification>
- `sections/page-affiliates.liquid` has D-10 hero, 3 tier cards in md:grid-cols-3 with correct commission values and card styles, UpPromote iframe with lazy loading, schema present with upromote_url setting
</verification>

<success_criteria>
Affiliates page renders 3 commission tier cards with correct styles (Standard/Silver/Gold), Gold card is inverted bg-deep, commission % is coral. UpPromote iframe embeds when URL is configured; apply button shows as fallback when URL is blank.
</success_criteria>

<output>
Create `.planning/phases/05-content-pages/05-04-SUMMARY.md` when done.
</output>
