---
phase: 05-content-pages
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/page-social.liquid
autonomous: true
requirements:
  - PAGE-06

must_haves:
  truths:
    - "Social UGC page renders D-10 hero with 'As Seen On You' H1"
    - "UGC grid iterates shop.metaobjects.social_post.values in a 4-column grid"
    - "Each post card shows square image with hover overlay and caption"
    - "Cards tagged with a product show 'Shop This Look' link in overlay"
    - "Empty state renders 'Nothing to see yet.' heading and @soleilnoir tag-us copy"
    - "Section has {% schema %} block"
  artifacts:
    - path: sections/page-social.liquid
      provides: Full-page UGC gallery section iterating social_post metaobject
      contains: "{% schema %}"
  key_links:
    - from: sections/page-social.liquid
      to: shop.metaobjects.social_post.values
      via: "{%- assign posts = shop.metaobjects.social_post.values -%}"
      pattern: "shop.metaobjects.social_post"
---

<objective>
Build the Social UGC gallery page section — a full-page grid of `social_post` metaobject entries with hover overlays and shoppable links, per D-04 and D-10.

Purpose: Surface community content as a shoppable gallery that turns UGC into product discovery.
Output: `sections/page-social.liquid`
</objective>

<execution_context>
@F:/CODING/BikinniSite/.claude/get-shit-done/workflows/execute-plan.md
@F:/CODING/BikinniSite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CODING/BikinniSite/.planning/ROADMAP.md
@F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md

<interfaces>
<!-- social_post metaobject fields (from Phase 2 META-03 definition): -->
<!--   .image (file_reference), .caption (single_line_text), .product (product_reference) -->
<!-- Iteration pattern from sections/social-feed.liquid: -->
<!--   {%- assign posts = shop.metaobjects.social_post.values -%} -->
<!-- D-04 specs: -->
<!-- Grid: grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4 -->
<!-- Card: relative group aspect-square overflow-hidden bg-sand -->
<!-- Image: w-full h-full object-cover scale-100 group-hover:scale-105 transition-transform duration-500 -->
<!-- Overlay: absolute inset-0 bg-deep/60 flex flex-col items-center justify-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300 -->
<!-- Caption: font-body text-xs text-cream text-center px-4 line-clamp-2 -->
<!-- Shop link: font-body text-xs uppercase tracking-widest text-cream border border-cream/50 px-4 py-2 hover:bg-cream hover:text-deep transition-colors -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build page-social.liquid</name>
  <files>sections/page-social.liquid</files>
  <read_first>
    - F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md (D-04, D-10)
    - F:/CODING/BikinniSite/sections/social-feed.liquid (metaobject iteration and card pattern)
  </read_first>
  <action>
Create `sections/page-social.liquid` implementing D-04 and D-10. This is a FULL-PAGE gallery section — distinct from the home page `social-feed.liquid` strip, which is a horizontal scroll. This section uses a grid layout.

Structure:

1. Page hero (D-10): `<section class="bg-deep text-cream py-16 lg:py-24">`. H1 `font-display text-4xl lg:text-6xl font-normal leading-tight text-center` renders `{{ section.settings.heading | escape }}`, default "As Seen On You". Subtitle `font-body text-xs uppercase tracking-widest text-cream/60 mt-4 text-center` renders `{{ section.settings.subtitle | escape }}`, default "Real bodies. Real styles. All Soleil Noir." Show subtitle only if non-blank.

2. Gallery section: `<section class="max-w-screen-xl mx-auto px-4 lg:px-8 py-12 lg:py-16">`.

   `{%- assign posts = shop.metaobjects.social_post.values -%}`

   If `posts.size > 0`:
   Grid: `<div class="grid grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4">`.

   For each post: `{%- for post in posts -%}`

   Card: `<div class="relative group aspect-square overflow-hidden bg-sand">`.

   Image: if `post.image != blank`, render `{{ post.image | image_url: width: 600 | image_tag: class: 'w-full h-full object-cover scale-100 group-hover:scale-105 transition-transform duration-500', loading: 'lazy', alt: post.caption }}`. Else: `<div class="w-full h-full bg-sand"></div>`.

   Overlay: `<div class="absolute inset-0 bg-deep/60 flex flex-col items-center justify-center gap-2 opacity-0 group-hover:opacity-100 transition-opacity duration-300">`.

   Inside overlay:
   - Caption (always): `<p class="font-body text-xs text-cream text-center px-4 line-clamp-2">{{ post.caption | escape }}</p>`.
   - Shop link (if product tagged): `{%- if post.product != blank -%}<a href="{{ post.product.url | escape }}" class="font-body text-xs uppercase tracking-widest text-cream border border-cream/50 px-4 py-2 hover:bg-cream hover:text-deep transition-colors">Shop This Look</a>{%- endif -%}`.

   Close overlay div, close card div, close loop.

   Else (empty state):
   ```
   <div class="py-24 text-center">
     <h2 class="font-display text-3xl font-normal text-deep">Nothing to see yet.</h2>
     <p class="font-body text-sm text-mid leading-relaxed mt-4">Tag us @soleilnoir to be featured here.</p>
   </div>
   ```

Schema settings: `heading` (text, default "As Seen On You"), `subtitle` (text, default "Real bodies. Real styles. All Soleil Noir."). Schema name: "Social UGC Gallery". Presets: one preset named "Social UGC Gallery".

Security: `post.caption` uses `| escape`. `post.product.url` uses `| escape`. Image rendered via `image_tag` filter (Shopify-sanitized). `section.settings.*` uses `| escape`.
  </action>
  <verify>
    <automated>grep -c "{% schema %}\|shop.metaobjects.social_post\|grid-cols-2 lg:grid-cols-3 xl:grid-cols-4\|Shop This Look" F:/CODING/BikinniSite/sections/page-social.liquid</automated>
  </verify>
  <acceptance_criteria>
    - File `sections/page-social.liquid` exists and is non-empty
    - `grep "{% schema %}" sections/page-social.liquid` returns a match
    - `grep "shop.metaobjects.social_post.values" sections/page-social.liquid` returns a match
    - `grep "grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-4" sections/page-social.liquid` returns a match
    - `grep "aspect-square overflow-hidden" sections/page-social.liquid` returns a match
    - `grep "group-hover:scale-105" sections/page-social.liquid` returns a match
    - `grep "opacity-0 group-hover:opacity-100" sections/page-social.liquid` returns a match
    - `grep "Shop This Look" sections/page-social.liquid` returns a match
    - `grep "Nothing to see yet" sections/page-social.liquid` returns a match
    - `grep "| escape" sections/page-social.liquid` returns at least 3 matches
  </acceptance_criteria>
  <done>page-social.liquid renders D-10 hero, UGC grid in D-04 layout, hover overlay with caption and conditional shop link, and empty state; all outputs escaped; schema present</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify admin → metaobject | social_post entries created by merchant |
| Liquid → HTML output | Caption and product URL rendered into page HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-05-01 | Tampering | post.caption rendered in overlay | mitigate | `| escape` applied to caption output |
| T-05-05-02 | Tampering | post.product.url in anchor href | mitigate | `| escape` applied; Shopify product URLs are internal and path-safe |
| T-05-05-03 | Information Disclosure | image alt from post.caption | accept | Caption is merchant-entered; alt is informational only, not sensitive |
</threat_model>

<verification>
- `sections/page-social.liquid` iterates `shop.metaobjects.social_post.values`, renders D-04 grid, hover overlay, "Shop This Look" link, empty state copy, schema present
</verification>

<success_criteria>
Social page renders a 4-column UGC grid (responsive: 2→3→4 cols). Each card shows image with hover overlay, caption, and "Shop This Look" link when product is tagged. Empty state shows when no posts exist.
</success_criteria>

<output>
Create `.planning/phases/05-content-pages/05-05-SUMMARY.md` when done.
</output>
