---
phase: 05-content-pages
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - sections/page-about.liquid
  - sections/page-payment.liquid
autonomous: true
requirements:
  - PAGE-01
  - PAGE-03

must_haves:
  truths:
    - "About page renders a bg-deep hero with 'Our Story' H1 and subtitle"
    - "About story section renders merchant-editable body text in a max-w-3xl prose container"
    - "About page has optional team blocks via schema block array"
    - "Payment info page renders a max-w-2xl column with payment icon row and trust badge strip"
    - "Both sections have {% schema %} blocks with heading/subtitle settings"
  artifacts:
    - path: sections/page-about.liquid
      provides: About page section with hero + story + optional team blocks
      contains: "{% schema %}"
    - path: sections/page-payment.liquid
      provides: Payment & Security section with icons + trust badges
      contains: "{% schema %}"
  key_links:
    - from: sections/page-about.liquid
      to: "{% schema %}"
      via: schema block at bottom of file
    - from: sections/page-payment.liquid
      to: "{% schema %}"
      via: schema block at bottom of file
---

<objective>
Build two standalone content page sections: the About page and the Payment Info page.

Purpose: Deliver the brand story and payment trust content as merchant-editable Shopify sections.
Output: `sections/page-about.liquid`, `sections/page-payment.liquid`
</objective>

<execution_context>
@F:/CODING/BikinniSite/.claude/get-shit-done/workflows/execute-plan.md
@F:/CODING/BikinniSite/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@F:/CODING/BikinniSite/.planning/ROADMAP.md
@F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md

<interfaces>
<!-- Established section schema pattern (from sections/hero.liquid, sections/featured-products.liquid) -->
<!-- Every section ends with {% schema %} ... {% endschema %} block -->
<!-- Tailwind container: max-w-screen-xl mx-auto px-4 lg:px-8 py-12 lg:py-16 -->
<!-- Prose container: max-w-3xl mx-auto px-4 py-16 -->
<!-- Page hero (D-10): bg-deep text-cream py-16 lg:py-24 -->
<!-- H1: font-display text-4xl lg:text-6xl font-normal leading-tight text-center -->
<!-- Subtitle: font-body text-xs uppercase tracking-widest text-cream/60 mt-4 text-center -->
<!-- All Liquid output: {{ value | escape }} unless it is Shopify-sanitized rich text -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build page-about.liquid</name>
  <files>sections/page-about.liquid</files>
  <read_first>
    - F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md (D-08, D-10)
    - F:/CODING/BikinniSite/sections/hero.liquid (schema pattern reference)
    - F:/CODING/BikinniSite/sections/featured-products.liquid (blocks pattern reference)
  </read_first>
  <action>
Create `sections/page-about.liquid` implementing D-08 and D-10.

Structure:

1. Page hero (D-10): `<section class="bg-deep text-cream py-16 lg:py-24">` wrapping `<div class="max-w-screen-xl mx-auto px-4 text-center">`. H1: `font-display text-4xl lg:text-6xl font-normal leading-tight text-center` rendering `{{ section.settings.heading | escape }}`. Optional subtitle: `font-body text-xs uppercase tracking-widest text-cream/60 mt-4 text-center` rendering `{{ section.settings.subtitle | escape }}` — only rendered if `section.settings.subtitle != blank`.

   Hard-coded default heading per copywriting contract: "Our Story". Default subtitle: "Built in the US. Designed for every body."

2. Story section (D-08): `<section class="max-w-3xl mx-auto px-4 py-16">`. Body copy from `section.settings.story` (textarea schema setting), rendered as `<p class="font-body text-sm text-mid leading-relaxed">{{ section.settings.story | escape }}</p>`. Include a primary CTA link (`section.settings.cta_url`, `section.settings.cta_label`) rendered as `<a href="{{ section.settings.cta_url | escape }}" class="inline-block mt-8 bg-deep text-cream font-body text-xs tracking-widest uppercase py-4 px-8 hover:opacity-90 transition-opacity">{{ section.settings.cta_label | escape }}</a>`. Show CTA only if `section.settings.cta_url != blank`. Default CTA label: "Meet Our Models".

3. Team blocks (D-08): `<section class="max-w-screen-xl mx-auto px-4 lg:px-8 py-12 lg:py-16">`. If `section.blocks.size > 0`, render a grid `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8`. Each block (type: `team_member`) renders: portrait image `aspect-[2/3] overflow-hidden` with `object-cover w-full h-full`; name `font-display text-2xl font-normal text-deep p-4`; role `font-body text-xs uppercase tracking-widest text-mid mt-1 px-4 pb-4`. Block settings: `image` (image_picker), `name` (text), `role` (text). Image rendered via `{{ block.settings.image | image_url: width: 600 | image_tag: class: 'object-cover w-full h-full', loading: 'lazy', alt: block.settings.name | escape }}`.

Schema settings: `heading` (text, default "Our Story"), `subtitle` (text, default "Built in the US. Designed for every body."), `story` (textarea, label "Brand story copy"), `cta_label` (text, default "Meet Our Models"), `cta_url` (url). Schema blocks: type `team_member`, name "Team Member", max_blocks 6, settings: `name` (text), `role` (text), `image` (image_picker). Schema name: "About Page". Add presets array with one preset named "About Page".

Security: all `{{ section.settings.* }}` outputs use `| escape`. Image tags use Shopify `image_tag` filter (XSS-safe). `cta_url` rendered with `| escape`.
  </action>
  <verify>
    <automated>grep -c "data-accordion\|{% schema %}\|bg-deep text-cream py-16" F:/CODING/BikinniSite/sections/page-about.liquid</automated>
  </verify>
  <acceptance_criteria>
    - File `sections/page-about.liquid` exists and is non-empty
    - `grep "{% schema %}" sections/page-about.liquid` returns a match
    - `grep "bg-deep text-cream py-16 lg:py-24" sections/page-about.liquid` returns a match
    - `grep "font-display text-4xl lg:text-6xl" sections/page-about.liquid` returns a match
    - `grep "max-w-3xl mx-auto px-4 py-16" sections/page-about.liquid` returns a match
    - `grep "grid-cols-1 sm:grid-cols-2 lg:grid-cols-3" sections/page-about.liquid` returns a match
    - `grep "team_member" sections/page-about.liquid` returns a match
    - `grep "| escape" sections/page-about.liquid` returns at least 3 matches
  </acceptance_criteria>
  <done>page-about.liquid renders D-08 hero, story section, and optional team block grid; schema is present with all settings and block type</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build page-payment.liquid</name>
  <files>sections/page-payment.liquid</files>
  <read_first>
    - F:/CODING/BikinniSite/.planning/phases/05-content-pages/05-UI-SPEC.md (D-09, D-10)
    - F:/CODING/BikinniSite/sections/hero.liquid (schema pattern reference)
  </read_first>
  <action>
Create `sections/page-payment.liquid` implementing D-09 and D-10.

Structure:

1. Page hero (D-10): Same pattern as Task 1 hero — `<section class="bg-deep text-cream py-16 lg:py-24">`. H1 renders `{{ section.settings.heading | escape }}` default "Payment & Security". Subtitle renders `{{ section.settings.subtitle | escape }}` default "Safe, simple, US checkout — always." Subtitle only rendered if non-blank.

2. Content column (D-09): `<section class="max-w-2xl mx-auto px-4 py-16">`.

   a. Body copy from `section.settings.body` (textarea), rendered as `<p class="font-body text-sm text-mid leading-relaxed">{{ section.settings.body | escape }}</p>`.

   b. Payment icons strip: `<div class="flex flex-wrap gap-6 items-center mt-6">`. Iterate `section.blocks` of type `payment_icon`. Each block renders either a merchant-uploaded image (`block.settings.icon_image`, rendered via `image_tag` filter, `class="h-8 w-auto"`, loading lazy) or, if image is blank, a text label `<span class="font-body text-xs uppercase tracking-widest text-mid">{{ block.settings.label | escape }}</span>`. Block settings: `label` (text, label "Payment method label"), `icon_image` (image_picker, label "Icon image (optional)").

   c. Trust badge strip: `<div class="font-body text-xs uppercase tracking-widest text-mid flex flex-wrap gap-8 items-center mt-6">`. Three hardcoded badges: "SSL Encrypted", "PCI-DSS Level 1", "US-Based Business" — each in a `<span>`.

Schema settings: `heading` (text, default "Payment & Security"), `subtitle` (text, default "Safe, simple, US checkout — always."), `body` (textarea, label "Body copy"). Schema blocks: type `payment_icon`, name "Payment Icon", max_blocks 8, settings: `label` (text), `icon_image` (image_picker). Schema name: "Payment Info Page". Add presets with one preset named "Payment Info".

Security: all `{{ section.settings.* }}` uses `| escape`; `image_tag` filter is XSS-safe.
  </action>
  <verify>
    <automated>grep -c "{% schema %}\|max-w-2xl\|trust\|PCI-DSS" F:/CODING/BikinniSite/sections/page-payment.liquid</automated>
  </verify>
  <acceptance_criteria>
    - File `sections/page-payment.liquid` exists and is non-empty
    - `grep "{% schema %}" sections/page-payment.liquid` returns a match
    - `grep "bg-deep text-cream py-16 lg:py-24" sections/page-payment.liquid` returns a match
    - `grep "max-w-2xl mx-auto px-4 py-16" sections/page-payment.liquid` returns a match
    - `grep "flex flex-wrap gap-6 items-center mt-6" sections/page-payment.liquid` returns a match
    - `grep "PCI-DSS" sections/page-payment.liquid` returns a match
    - `grep "payment_icon" sections/page-payment.liquid` returns a match
    - `grep "| escape" sections/page-payment.liquid` returns at least 2 matches
  </acceptance_criteria>
  <done>page-payment.liquid renders D-09 hero, body copy, payment icon blocks, and hardcoded trust badges; schema is present with settings and payment_icon block type</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Merchant → Shopify admin | Section settings and block content entered in theme editor |
| Liquid → HTML output | Settings values rendered into page HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | Tampering | section.settings.* outputs | mitigate | All settings rendered with `| escape` filter |
| T-05-01-02 | Tampering | section.settings.cta_url | mitigate | Rendered with `| escape`; Shopify sanitizes url-type settings |
| T-05-01-03 | Information Disclosure | image_tag alt attributes | mitigate | Alt text from settings rendered via `image_tag` with escaped alt param |
</threat_model>

<verification>
- `sections/page-about.liquid` exists, has `{% schema %}`, hero classes, story prose, team grid, and `| escape` on all settings outputs
- `sections/page-payment.liquid` exists, has `{% schema %}`, hero classes, max-w-2xl column, payment icon blocks, and PCI-DSS trust badge
</verification>

<success_criteria>
Both sections render correctly in `shopify theme dev` preview when assigned to pages. Schema settings appear in theme editor. No unsanitized output.
</success_criteria>

<output>
Create `.planning/phases/05-content-pages/05-01-SUMMARY.md` when done.
</output>
