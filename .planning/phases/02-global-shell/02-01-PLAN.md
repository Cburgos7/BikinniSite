---
phase: 02-global-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - shopify.theme.toml
autonomous: false
requirements:
  - NAV-01
user_setup:
  - service: shopify-partner
    why: "Dev store required for local preview and all Shopify admin tasks in Phase 2"
    env_vars: []
    dashboard_config:
      - task: "Create Shopify Partner account"
        location: "partners.shopify.com → Sign up (free)"
      - task: "Create Development store"
        location: "Partners dashboard → Stores → Add store → Development store"
      - task: "Install Shopify CLI"
        location: "Terminal: npm install -g @shopify/cli @shopify/theme"
      - task: "Authenticate CLI"
        location: "Terminal: shopify auth login --store=<dev-store>.myshopify.com"
      - task: "Add 3 placeholder products with size variants, Best Sellers + Bikinis + Lingerie collections"
        location: "Shopify admin → Products and Collections"

must_haves:
  truths:
    - "Developer can run `shopify theme dev` and see the theme in a live preview URL"
    - "shopify.theme.toml has the dev store environment configured so no extra flags are needed"
    - "Announcement bar is confirmed visible in the preview"
  artifacts:
    - path: "shopify.theme.toml"
      provides: "[environments.development] with store URL set"
      contains: "environments"
  key_links:
    - from: "shopify.theme.toml"
      to: "<dev-store>.myshopify.com"
      via: "shopify theme dev"
      pattern: "store\\s*="
---

<objective>
Set up the Shopify development environment so all Phase 2 Liquid/section work can be previewed live.

Purpose: Without a dev store and working `shopify theme dev`, no code changes in Phase 2 can be visually verified. This is a blocker for all Wave 2 plans.
Output: Working local preview URL; `shopify.theme.toml` configured for dev store; placeholder store content present.
</objective>

<execution_context>
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\PROJECT.md
@F:\CODING\BikinniSite\.planning\ROADMAP.md
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Shopify Partner account + dev store + CLI setup</name>
  <what-built>
    Nothing is built yet — this is the owner performing account and environment setup that Claude cannot do on their behalf.
  </what-built>
  <how-to-verify>
    Complete these steps in order:

    1. Go to https://partners.shopify.com and create a free Partner account (use burgoschris7@gmail.com or a dedicated business email).
    2. Inside the Partner dashboard → Stores → Add store → select "Development store". Name it something like "soleil-noir-dev". Note the store URL: `<name>.myshopify.com`.
    3. In your terminal, run: `npm install -g @shopify/cli @shopify/theme`
    4. Run: `shopify auth login --store=<dev-store>.myshopify.com` and complete the browser-based OAuth flow.
    5. In Shopify admin for the dev store, add placeholder content:
       - Create 3 products, each with size variants XS / S / M / L / XL. Assign all 3 to a collection named exactly "Best Sellers".
       - Create a collection named exactly "Bikinis" — assign at least 1 product.
       - Create a collection named exactly "Lingerie" — assign at least 1 product.
    6. Tell Claude your dev store URL (the `<name>.myshopify.com` part only).
  </how-to-verify>
  <resume-signal>Provide the dev store URL (e.g. "soleil-noir-dev.myshopify.com") to continue.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure shopify.theme.toml with dev store environment</name>
  <files>shopify.theme.toml</files>
  <read_first>
    - `shopify.theme.toml` — read current content before modifying; it was scaffolded in Phase 1
  </read_first>
  <action>
    Read the current `shopify.theme.toml`. Add or update the `[environments.development]` block with the dev store URL provided by the owner in Task 1. The block must set `store` to the dev store URL and `theme` to a development theme name (e.g. "Soleil Noir Dev"). If a `[environments.development]` block already exists, update the `store` value. Do not remove any existing environment blocks.

    Target shape for the development block:

    ```
    [environments.development]
    store = "<dev-store>.myshopify.com"
    theme = "Soleil Noir Dev"
    ```

    Replace `<dev-store>` with the actual URL from Task 1. Per D-16, the goal is that `shopify theme dev` works without extra flags.
  </action>
  <acceptance_criteria>
    - `shopify.theme.toml` contains a line matching `store = "*.myshopify.com"` inside `[environments.development]`
    - `shopify.theme.toml` contains `theme = "Soleil Noir Dev"` inside `[environments.development]`
    - Running `shopify theme dev` (no extra flags) connects to the dev store without an error about missing store config
  </acceptance_criteria>
  <verify>
    <automated>grep -n "environments.development" shopify.theme.toml && grep -n "myshopify.com" shopify.theme.toml</automated>
  </verify>
  <done>shopify.theme.toml has `[environments.development]` with correct store URL and theme name; `shopify theme dev` launches the preview without extra flags.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer machine → Shopify CLI | OAuth token stored by CLI; no secrets in repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | shopify.theme.toml | mitigate | Store URL only (no secrets) in toml; never commit `shopify.app.toml` auth tokens |
| T-02-SC | Tampering | npm install -g @shopify/cli | mitigate | Install from official npmjs.com/package/@shopify/cli; verify package name before running |
</threat_model>

<verification>
1. `grep -n "environments.development" shopify.theme.toml` returns a match
2. `shopify theme dev` connects to the dev store and prints a preview URL
3. Opening the preview URL in a browser shows the theme (announcement bar visible from Phase 1)
</verification>

<success_criteria>
- shopify.theme.toml has [environments.development] with dev store URL
- `shopify theme dev` runs without extra flags and returns a shareable preview URL
- At least one product is visible in Shopify admin with size variants and collection membership
</success_criteria>

<output>
Create `.planning/phases/02-global-shell/02-01-SUMMARY.md` when done
</output>
