---
phase: 02-global-shell
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements:
  - META-01
  - META-02
  - META-03
  - META-04

must_haves:
  truths:
    - "All 4 product metafields are accessible in Liquid via product.metafields.custom.*"
    - "The collection metafield hero_subtitle is accessible via collection.metafields.custom.hero_subtitle"
    - "The model metaobject definition exists and has all 7 fields"
    - "The social_post metaobject definition exists and has all 4 fields"
  artifacts:
    - path: "Shopify admin — Settings → Custom data → Products"
      provides: "care_instructions, model_sizing, fabric_composition, coverage_level metafields"
    - path: "Shopify admin — Settings → Custom data → Collections"
      provides: "hero_subtitle metafield"
    - path: "Shopify admin — Settings → Custom data → Metaobjects"
      provides: "model and social_post definitions"
  key_links:
    - from: "product.metafields.custom.care_instructions"
      to: "PDP accordion (Phase 4)"
      via: "Liquid metafield accessor"
      pattern: "metafields\\.custom\\.care_instructions"
    - from: "metaobject: model"
      to: "Models page grid (Phase 5)"
      via: "for metaobject in shop.metaobjects.model.values"
      pattern: "metaobjects\\.model"
---

<objective>
Define all Shopify metafields and metaobject schemas in the Shopify admin so Phase 4 and Phase 5 Liquid code can reference them without 404/nil errors.

Purpose: Metafield and metaobject definitions must exist before any Liquid template tries to read them. Defining them now in Phase 2 unblocks PDP, collection pages, Models page, and Social page in later phases.
Output: 5 metafield definitions + 2 metaobject definitions created in Shopify admin (no code changes — all admin configuration).
</objective>

<execution_context>
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-UI-SPEC.md
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-CONTEXT.md
</context>

<tasks>

<task type="checkpoint:human-action">
  <name>Task 1: Create product and collection metafield definitions in Shopify admin</name>
  <what-built>
    Nothing to build in code. Owner creates metafield definitions in Shopify admin Settings panel.
  </what-built>
  <how-to-verify>
    Go to the Shopify admin for the dev store → Settings → Custom data.

    Under "Products", add these 4 metafield definitions (namespace must be exactly `custom`):

    | Name (label) | Namespace.Key | Type |
    |---|---|---|
    | Care Instructions | custom.care_instructions | Multi-line text |
    | Model Sizing | custom.model_sizing | Single-line text |
    | Fabric Composition | custom.fabric_composition | Single-line text |
    | Coverage Level | custom.coverage_level | Single-line text |

    Under "Collections", add 1 metafield definition:

    | Name (label) | Namespace.Key | Type |
    |---|---|---|
    | Hero Subtitle | custom.hero_subtitle | Single-line text |

    To confirm: open any product in Shopify admin → scroll to Metafields section → verify all 4 fields appear as empty inputs.
    Open any collection → verify hero_subtitle appears as an empty input.
  </how-to-verify>
  <resume-signal>Type "metafields done" once all 5 definitions are visible on product and collection edit pages.</resume-signal>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Create model and social_post metaobject definitions in Shopify admin</name>
  <what-built>
    Nothing to build in code. Owner creates metaobject definitions in Shopify admin.
  </what-built>
  <how-to-verify>
    Go to Shopify admin → Settings → Custom data → Metaobjects → Add definition.

    **Definition 1: model**
    - Name: `model` (API identifier will auto-set to `model`)
    - Add these fields in order:

    | Field name | API identifier | Type | Required |
    |---|---|---|---|
    | Name | name | Single-line text | Yes |
    | Photo | photo | File (Images only) | Yes |
    | Bio | bio | Multi-line text | Yes |
    | Height | height | Single-line text | Yes |
    | Size Worn | size_worn | Single-line text | Yes |
    | Instagram Handle | instagram_handle | Single-line text | No |
    | Featured Products | featured_products | Product (List — multiple) | No |

    Save the definition.

    **Definition 2: social_post**
    - Name: `social_post` (API identifier: `social_post`)
    - Add these fields:

    | Field name | API identifier | Type | Required |
    |---|---|---|---|
    | Image | image | File (Images only) | Yes |
    | Caption | caption | Single-line text | Yes |
    | Link | link | URL | Yes |
    | Product Tag | product_tag | Product | No |

    Save the definition.

    To confirm: both definitions appear under Metaobjects in Settings → Custom data. Click into each and verify the field count matches (model: 7 fields, social_post: 4 fields).
  </how-to-verify>
  <resume-signal>Type "metaobjects done" once both definitions are saved and field counts confirmed.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Shopify admin UI → Shopify metafield store | Admin-only configuration; no code surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-02 | Tampering | Metafield namespace "custom" | mitigate | Use exactly `custom` namespace — non-standard namespaces would break all Phase 4/5 Liquid accessors |
| T-02-03 | Information Disclosure | model.instagram_handle | accept | Handle is public-facing by design; no PII concern |
</threat_model>

<verification>
1. Shopify admin → any product edit page → Metafields section shows: care_instructions, model_sizing, fabric_composition, coverage_level
2. Shopify admin → any collection edit page → Metafields section shows: hero_subtitle
3. Shopify admin → Settings → Custom data → Metaobjects → "model" definition has 7 fields
4. Shopify admin → Settings → Custom data → Metaobjects → "social_post" definition has 4 fields
</verification>

<success_criteria>
- All 4 product metafields defined under namespace "custom" and visible on product edit pages
- Collection metafield hero_subtitle defined under namespace "custom" and visible on collection edit pages
- Metaobject "model" definition exists with all 7 fields (name, photo, bio, height, size_worn, instagram_handle, featured_products)
- Metaobject "social_post" definition exists with all 4 fields (image, caption, link, product_tag)
</success_criteria>

<output>
Create `.planning/phases/02-global-shell/02-02-SUMMARY.md` when done
</output>
