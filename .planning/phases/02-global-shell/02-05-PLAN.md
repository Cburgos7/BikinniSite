---
phase: 02-global-shell
plan: 05
type: execute
wave: 3
depends_on:
  - 02-03
  - 02-04
files_modified:
  - sections/cookie-consent.liquid
  - assets/cookie-consent.js
  - templates/404.json
  - sections/not-found.liquid
  - layout/theme.liquid
autonomous: true
requirements:
  - NAV-04
  - NAV-05

must_haves:
  truths:
    - "CCPA banner appears on first visit, fixed at bottom; dismisses on either Accept or Decline"
    - "Once a choice is made, banner never shows again on that browser (localStorage persists)"
    - "404 page shows the 404 numeral, brand copy 'Looks like this page took a swim.', and two CTAs"
    - "Announcement bar, nav, and footer all render on the 404 page (it uses the same theme shell)"
  artifacts:
    - path: "sections/cookie-consent.liquid"
      provides: "CCPA banner markup, schema"
      contains: "cookie-consent"
    - path: "assets/cookie-consent.js"
      provides: "localStorage check, show/hide, Accept/Decline handlers"
      exports: ["default"]
    - path: "templates/404.json"
      provides: "404 template JSON pointing to not-found section"
      contains: "not-found"
    - path: "sections/not-found.liquid"
      provides: "404 page markup with brand copy and CTAs"
      contains: "took a swim"
  key_links:
    - from: "layout/theme.liquid"
      to: "sections/cookie-consent.liquid"
      via: "{% section 'cookie-consent' %}"
      pattern: "cookie-consent"
    - from: "templates/404.json"
      to: "sections/not-found.liquid"
      via: "section type reference"
      pattern: "not-found"
---

<objective>
Build the CCPA cookie consent banner and the 404 page — both are global shell elements that appear on every page or serve as the fallback for broken URLs.

Purpose: CCPA compliance requires the consent banner at launch. The 404 page prevents broken URLs from showing Shopify's generic error page.
Output: `sections/cookie-consent.liquid` + `assets/cookie-consent.js`, `templates/404.json` + `sections/not-found.liquid`, and `layout/theme.liquid` updated to include the consent section.
</objective>

<execution_context>
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-UI-SPEC.md
@F:\CODING\BikinniSite\.planning\phases\01-theme-foundation\01-CONTEXT.md

<interfaces>
<!-- localStorage API -->
localStorage.setItem('soleil_noir_cookie_consent', 'accepted')  // or 'declined'
localStorage.getItem('soleil_noir_cookie_consent')  // returns null if not set

<!-- 404 template format (Shopify JSON template) -->
{
  "sections": {
    "main": { "type": "not-found", "settings": {} }
  },
  "order": ["main"]
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CCPA cookie consent banner — section + JS module</name>
  <files>sections/cookie-consent.liquid, assets/cookie-consent.js, layout/theme.liquid</files>
  <read_first>
    - `layout/theme.liquid` — current state after Plan 03 and 04 edits; must be read to find correct insertion point
    - `sections/announcement-bar.liquid` — schema and Tailwind pattern
    - `.planning/phases/02-global-shell/02-UI-SPEC.md` — Component 4: CCPA Cookie Banner spec
  </read_first>
  <behavior>
    sections/cookie-consent.liquid:
    - Wrapper: `<div id="cookie-consent-banner" class="fixed bottom-0 left-0 right-0 z-[100] bg-deep text-cream transform translate-y-0 transition-transform duration-300">` — starts visible; JS hides it on load if consent exists
    - Inner: `<div class="max-w-7xl mx-auto flex flex-col sm:flex-row items-center justify-between px-6 py-3 gap-3">` with text `<p class="font-body text-sm text-cream">We use cookies to improve your experience and for analytics.</p>` and button row
    - Accept button: `id="cookie-accept"` `class="bg-coral text-cream px-4 py-2 text-xs font-body uppercase tracking-wider"` text "Accept"
    - Decline button: `id="cookie-decline"` `class="border border-cream text-cream px-4 py-2 text-xs font-body"` text "Decline"
    - Accessibility: `role="complementary"` `aria-label="Cookie consent"` on the outer wrapper
    - Schema: name "Cookie Consent", no editable settings needed (copy is fixed per legal requirement); presets include "Cookie Consent"
    - Script: `<script type="module" src="{{ 'cookie-consent.js' | asset_url }}"></script>`

    assets/cookie-consent.js:
    - On DOMContentLoaded:
      1. Check `localStorage.getItem('soleil_noir_cookie_consent')` — if not null, immediately hide banner: `banner.classList.add('translate-y-full')`, then after 300ms `banner.style.display = 'none'`
      2. Bind `#cookie-accept` click: store `localStorage.setItem('soleil_noir_cookie_consent', 'accepted')`; dismiss banner (animate then hide)
      3. Bind `#cookie-decline` click: store `localStorage.setItem('soleil_noir_cookie_consent', 'declined')`; dismiss banner (animate then hide)
    - Dismiss animation: add `translate-y-full` class on banner element, wait 300ms, then set `banner.style.display = 'none'`
    - Export default init function; call immediately

    layout/theme.liquid:
    - Add `{% section 'cookie-consent' %}` just before `</body>` (after the existing script tags)
  </behavior>
  <action>
    Create `sections/cookie-consent.liquid` with the banner markup, accessibility attributes, and schema as described. Create `assets/cookie-consent.js` with the localStorage check and dismiss logic. Edit `layout/theme.liquid` to include `{% section 'cookie-consent' %}` before `</body>`.
  </action>
  <verify>
    <automated>grep -n "cookie-consent-banner" sections/cookie-consent.liquid && grep -n "translate-y-full" sections/cookie-consent.liquid && grep -n "cookie-accept\|cookie-decline" sections/cookie-consent.liquid && grep -n "soleil_noir_cookie_consent" assets/cookie-consent.js && grep -n "translate-y-full" assets/cookie-consent.js && grep -n "cookie-consent" layout/theme.liquid</automated>
  </verify>
  <acceptance_criteria>
    - `sections/cookie-consent.liquid` exists with `id="cookie-consent-banner"` and `translate-y-full` transition class
    - Contains `id="cookie-accept"` button with `bg-coral` class
    - Contains `id="cookie-decline"` button with `border border-cream` class
    - Contains `role="complementary"` and `aria-label="Cookie consent"` on wrapper
    - `{% schema %}` block present with section name "Cookie Consent"
    - `assets/cookie-consent.js` exists with `export default`
    - `assets/cookie-consent.js` reads `localStorage.getItem('soleil_noir_cookie_consent')`
    - `assets/cookie-consent.js` writes `localStorage.setItem('soleil_noir_cookie_consent', 'accepted')` in accept handler
    - `assets/cookie-consent.js` writes `localStorage.setItem('soleil_noir_cookie_consent', 'declined')` in decline handler
    - `assets/cookie-consent.js` adds `translate-y-full` class to animate dismiss
    - `layout/theme.liquid` contains `{% section 'cookie-consent' %}` before `</body>`
  </acceptance_criteria>
  <done>CCPA banner renders at page bottom on first visit; Accept or Decline dismisses it with animation and stores choice in localStorage so it never appears again on that browser.</done>
</task>

<task type="auto">
  <name>Task 2: Build 404 template and not-found section</name>
  <files>templates/404.json, sections/not-found.liquid</files>
  <read_first>
    - `.planning/phases/02-global-shell/02-UI-SPEC.md` — Component 5: 404 Page layout and copy
    - `sections/announcement-bar.liquid` — schema pattern
    - Any existing `templates/404.json` if present (check before overwriting)
  </read_first>
  <behavior>
    templates/404.json:
    - Standard Shopify JSON template format:
      `{ "sections": { "main": { "type": "not-found", "settings": {} } }, "order": ["main"] }`

    sections/not-found.liquid:
    - Full-page centered layout: `<section class="min-h-screen flex flex-col items-center justify-center text-center px-6 bg-cream">`
    - Large decorative 404: `<p class="font-display text-[12rem] leading-none text-sand select-none" aria-hidden="true">404</p>` (aria-hidden because it's decorative; the h1 below carries the meaning)
    - Heading: `<h1 class="font-display text-2xl text-deep mt-2">Page Not Found</h1>`
    - Brand copy: `<p class="font-body text-mid mt-3">Looks like this page took a swim.</p>`
    - CTA row: `<div class="flex flex-col sm:flex-row gap-4 mt-8 justify-center">`
      - Primary: `<a href="/" class="bg-coral text-cream font-body uppercase tracking-widest px-8 py-3">BACK TO SHOP</a>`
      - Secondary: `<a href="/collections/all" class="border border-deep text-deep font-body uppercase tracking-widest px-8 py-3">VIEW COLLECTIONS</a>`
    - Schema: name "Not Found", no settings; presets include "Not Found"
    - No JS needed
  </behavior>
  <action>
    Create `templates/404.json` with the Shopify JSON template structure pointing to `not-found` section type. Create `sections/not-found.liquid` with the centered layout, decorative 404 numeral, brand copy, and two CTA buttons as specified. Include `{% schema %}` block. No JavaScript needed for this section.
  </action>
  <verify>
    <automated>grep -n "not-found" templates/404.json && grep -n "took a swim" sections/not-found.liquid && grep -n "font-display" sections/not-found.liquid && grep -n "schema" sections/not-found.liquid</automated>
  </verify>
  <acceptance_criteria>
    - `templates/404.json` exists and contains `"type": "not-found"`
    - `sections/not-found.liquid` exists and contains "Looks like this page took a swim."
    - Contains `<h1>` with "Page Not Found"
    - Contains decorative `404` text with `font-display` and `text-sand` classes
    - Contains primary CTA link to `/` with `bg-coral` class
    - Contains secondary CTA link to `/collections/all` with `border border-deep` class
    - `{% schema %}` block present
    - `aria-hidden="true"` on the decorative 404 numeral
  </acceptance_criteria>
  <done>Navigating to any invalid URL shows the 404 page with the decorative numeral, brand copy, and two CTAs inside the full theme shell (nav + footer).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser localStorage → cookie-consent.js | User preference stored locally; no server transmission |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-08 | Information Disclosure | localStorage soleil_noir_cookie_consent | accept | Stores only string "accepted" or "declined" — no PII, no session data |
| T-02-09 | Tampering | 404 page CTAs | accept | Both links are hardcoded in Liquid — not user-influenced |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `grep -n "cookie-consent" layout/theme.liquid` returns a match
2. `grep -n "soleil_noir_cookie_consent" assets/cookie-consent.js` returns a match
3. `grep -n "not-found" templates/404.json` returns a match
4. Visual: banner appears on fresh visit; disappears after Accept/Decline; does not reappear on next page load
5. Visual: navigating to `/this-does-not-exist` shows the 404 section with brand copy
</verification>

<success_criteria>
- CCPA banner shows on first visit and dismisses on Accept or Decline
- Banner never reappears on same browser after a choice is made
- 404 page renders inside full theme shell with brand copy and working CTAs
</success_criteria>

<output>
Create `.planning/phases/02-global-shell/02-05-SUMMARY.md` when done
</output>
