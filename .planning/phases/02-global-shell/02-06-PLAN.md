---
phase: 02-global-shell
plan: 06
type: execute
wave: 4
depends_on:
  - 02-03
files_modified:
  - sections/footer.liquid
  - layout/theme.liquid
  - templates/customers/login.liquid
  - templates/customers/register.liquid
  - templates/customers/account.liquid
  - templates/customers/addresses.liquid
  - templates/customers/order.liquid
autonomous: true
requirements:
  - AUTH-01
  - NAV-02

must_haves:
  truths:
    - "Footer renders below all page content with nav links to collections and placeholder policy links"
    - "Account icon in nav links to /account (Shopify routes); Shopify native login/register work"
    - "Customer account, login, register, order history, and address pages all render with brand styling"
    - "All customer templates use font-display / font-body / design tokens — no unstyled Shopify defaults"
  artifacts:
    - path: "sections/footer.liquid"
      provides: "Footer with navigation columns, brand name, copyright; schema for link customization"
      contains: "footer"
    - path: "templates/customers/login.liquid"
      provides: "Styled login page template"
      contains: "customer_login_url"
    - path: "templates/customers/account.liquid"
      provides: "Styled account overview page"
      contains: "customer.orders"
    - path: "templates/customers/addresses.liquid"
      provides: "Styled address management page"
      contains: "customer.addresses"
  key_links:
    - from: "layout/theme.liquid"
      to: "sections/footer.liquid"
      via: "{% section 'footer' %}"
      pattern: "section 'footer'"
    - from: "sections/header.liquid nav icon"
      to: "/account"
      via: "<a href='/account'>"
      pattern: "href.*account"
---

<objective>
Build the site footer and apply brand styling to all Shopify native customer account templates (login, register, account overview, addresses, order detail).

Purpose: Footer closes the page frame started by the nav. Account templates must carry brand identity — Shopify's default unstyled account pages would break the visual experience.
Output: `sections/footer.liquid` with stub nav columns; five customer account template files with Tailwind/brand-token styling.
</objective>

<execution_context>
@F:\CODING\BikinniSite\.claude\get-shit-done\workflows\execute-plan.md
@F:\CODING\BikinniSite\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-CONTEXT.md
@F:\CODING\BikinniSite\.planning\phases\02-global-shell\02-UI-SPEC.md
@F:\CODING\BikinniSite\.planning\phases\01-theme-foundation\01-CONTEXT.md

<interfaces>
<!-- Shopify customer Liquid objects -->
customer.first_name        — string
customer.email             — string
customer.orders            — array of order objects
customer.orders_count      — integer
customer.addresses         — array of address objects
customer.default_address   — address object

order.name                 — "#1001"
order.created_at           — date
order.financial_status     — "paid", "pending", etc.
order.fulfillment_status   — "fulfilled", "unfulfilled"
order.total_price          — money (use | money filter)
order.line_items           — array

address.first_name, address.last_name, address.address1, address.city, address.province, address.zip, address.country

<!-- Shopify account routes in Liquid -->
{{ routes.account_url }}         → /account
{{ routes.account_login_url }}   → /account/login
{{ routes.account_register_url }}→ /account/register
{{ routes.account_logout_url }}  → /account/logout
{{ routes.account_addresses_url }}→ /account/addresses

<!-- Form helpers -->
{% form 'customer_login' %}   — login form
{% form 'create_customer' %}  — register form
{% form 'customer_address', customer.new_address %} — new address
{% form 'customer_address', address %} — edit address

<!-- Note: Shopify native (non-headless) accounts. Templates live in templates/customers/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build sections/footer.liquid and include in theme.liquid</name>
  <files>sections/footer.liquid, layout/theme.liquid</files>
  <read_first>
    - `layout/theme.liquid` — current state after prior plans; find position after `</main>` for footer insertion
    - `sections/announcement-bar.liquid` — schema pattern
    - `.planning/phases/02-global-shell/02-CONTEXT.md` — Claude's Discretion note: "Footer layout and link structure (links will be stubbed — actual policy pages built in Phase 5)"
  </read_first>
  <behavior>
    Footer layout (Phase 5 will expand this; Phase 2 delivers a functional stub):
    - Outer: `<footer class="bg-deep text-cream py-12 mt-auto">` with `{% schema %}` at end
    - Inner container: `<div class="max-w-7xl mx-auto px-6 grid grid-cols-2 md:grid-cols-4 gap-8">`
    - Column 1 — Brand: `<div>` with shop name in `font-display text-xl` and tagline "Daring swim. Inclusive sizing." in `font-body text-mid text-sm mt-2`
    - Column 2 — Shop links: heading `<h3 class="font-body uppercase tracking-widest text-sm mb-4">Shop</h3>`, links to `/collections/new-in` (New In), `/collections/bikinis` (Bikinis), `/collections/lingerie` (Lingerie), `/collections/sale` (Sale)
    - Column 3 — Help links: heading "Help", links to `/pages/faq` (FAQ), `/pages/contact` (Contact), `/pages/size-guide` (Size Guide)
    - Column 4 — Legal links: heading "Legal", links to `/policies/privacy-policy` (Privacy), `/policies/terms-of-service` (Terms), `/policies/refund-policy` (Returns)
    - All links: `class="block font-body text-sm text-mid hover:text-cream transition-colors py-1"`
    - Copyright row: `<div class="mt-10 border-t border-mid/30 pt-6 text-center font-body text-xs text-mid">© {{ 'now' | date: '%Y' }} {{ shop.name }}. All rights reserved.</div>`
    - Schema: name "Footer", no custom settings needed at Phase 2 level; presets include "Footer"

    layout/theme.liquid:
    - Move `<main id="main-content">` to be inside a wrapper `<div class="flex flex-col min-h-screen">` and add `class="flex-1"` to `<main>`; add `{% section 'footer' %}` after `</main>` (before existing script tags)
    - If restructuring flex wrapper is too complex, simply add `{% section 'footer' %}` after `</main>` without flex wrapper — Phase 5 will refine layout
  </behavior>
  <action>
    Create `sections/footer.liquid` with the four-column grid layout, stub nav links, and copyright line as described. Include `{% schema %}` with section name "Footer". Edit `layout/theme.liquid` to add `{% section 'footer' %}` immediately after the closing `</main>` tag. Do not remove or reorder any existing sections.
  </action>
  <verify>
    <automated>grep -n "section 'footer'" layout/theme.liquid && grep -n "bg-deep" sections/footer.liquid && grep -n "collections/bikinis" sections/footer.liquid && grep -n "policies/privacy-policy" sections/footer.liquid && grep -n "schema" sections/footer.liquid</automated>
  </verify>
  <acceptance_criteria>
    - `sections/footer.liquid` exists with `<footer class="bg-deep text-cream` at root element
    - Contains links to `/collections/bikinis`, `/collections/lingerie`, `/collections/new-in`, `/collections/sale`
    - Contains link to `/policies/privacy-policy`
    - Contains `{{ 'now' | date: '%Y' }}` for dynamic copyright year
    - Contains `{% schema %}` block with name "Footer"
    - `layout/theme.liquid` contains `{% section 'footer' %}` after `</main>`
  </acceptance_criteria>
  <done>Footer renders at the bottom of every page with shop links, help links, legal links, and dynamic copyright year; editable via theme editor.</done>
</task>

<task type="auto">
  <name>Task 2: Style Shopify customer account templates</name>
  <files>templates/customers/login.liquid, templates/customers/register.liquid, templates/customers/account.liquid, templates/customers/addresses.liquid, templates/customers/order.liquid</files>
  <read_first>
    - `sections/announcement-bar.liquid` — establishes Tailwind class conventions
    - `.planning/phases/02-global-shell/02-CONTEXT.md` — "Shopify customer account template styling (standard Shopify native accounts, AUTH-01)" is Claude's Discretion
    - Check if `templates/customers/` directory exists and has any existing files before overwriting
  </read_first>
  <behavior>
    All templates share a common page wrapper for consistent padding:
    `<div class="max-w-2xl mx-auto px-6 py-12 min-h-screen bg-cream">`

    Headings: `<h1 class="font-display text-3xl text-deep mb-8">` per page

    Form inputs shared style: `class="w-full border border-mid px-4 py-3 font-body text-sm text-deep bg-cream focus:outline-none focus:border-deep mb-4"`
    Submit buttons: `class="w-full bg-coral text-cream font-body uppercase tracking-widest py-4 mt-2"`
    Links: `class="font-body text-sm text-mid hover:text-deep"`

    login.liquid:
    - Heading: "Welcome Back"
    - `{% form 'customer_login' %}` with email input, password input, submit "Sign In"
    - Link to `{{ routes.account_register_url }}`: "Don't have an account? Create one"
    - Error display: `{% if form.errors %}` → `<p class="text-coral font-body text-sm">{{ form.errors | default_errors }}</p>`

    register.liquid:
    - Heading: "Create Account"
    - `{% form 'create_customer' %}` with first_name, last_name, email, password inputs; submit "Create Account"
    - Link to `{{ routes.account_login_url }}`: "Already have an account? Sign in"
    - Error display same pattern

    account.liquid:
    - Heading: "My Account"
    - Welcome line: `font-body text-mid` "Welcome, {{ customer.first_name }}"
    - Orders section: `<h2 class="font-display text-xl mt-8 mb-4">Order History</h2>`; `{% if customer.orders_count == 0 %}` → "No orders yet." else table with columns: Order, Date, Payment Status, Fulfillment, Total
    - Table: `<table class="w-full font-body text-sm">` with `<th class="text-left py-2 border-b border-sand">` headers; `<td class="py-3 border-b border-sand text-mid">` cells
    - Order name is a link to `/account/orders/{{ order.id }}`: `class="text-deep hover:text-coral"`
    - Logout link: `<a href="{{ routes.account_logout_url }}" class="font-body text-sm text-mid hover:text-deep mt-8 block">Sign out</a>`

    addresses.liquid:
    - Heading: "Saved Addresses"
    - List existing addresses: `{% for address in customer.addresses %}` → card `<div class="border border-sand p-4 mb-4 font-body text-sm text-deep">` with address lines and Edit / Remove links
    - New address form: `{% form 'customer_address', customer.new_address %}` with standard input fields; submit "Add Address"

    order.liquid:
    - Heading: `"Order {{ order.name }}"`
    - Order meta: date, payment status, fulfillment status in `font-body text-mid text-sm`
    - Line items table same style as account.liquid orders table
    - Subtotal / shipping / total in `font-body text-sm text-deep` right-aligned
    - Back link: `<a href="{{ routes.account_url }}" class="font-body text-sm text-mid hover:text-deep">← Back to account</a>`
  </behavior>
  <action>
    Create all five files in `templates/customers/`. Each file is a standalone Liquid template (not a section-based JSON template — customer templates use `.liquid` format directly). Apply the shared wrapper, heading, form input, and button styles described in the behavior block. Use Tailwind utility classes throughout. Do not include `{% section %}` tags — customer `.liquid` templates render directly inside `layout/theme.liquid`.
  </action>
  <verify>
    <automated>grep -rn "customer_login" templates/customers/login.liquid && grep -rn "font-display" templates/customers/account.liquid && grep -rn "customer.orders" templates/customers/account.liquid && grep -rn "customer.addresses" templates/customers/addresses.liquid && grep -rn "bg-coral" templates/customers/login.liquid</automated>
  </verify>
  <acceptance_criteria>
    - `templates/customers/login.liquid` exists, contains `{% form 'customer_login' %}`, has `bg-coral` on submit button, shows form errors
    - `templates/customers/register.liquid` exists, contains `{% form 'create_customer' %}`, has `bg-coral` on submit button
    - `templates/customers/account.liquid` exists, contains `customer.orders_count` check, has order table for non-empty orders
    - `templates/customers/addresses.liquid` exists, contains `{% for address in customer.addresses %}` loop, has new address form
    - `templates/customers/order.liquid` exists, contains `order.name` display, has link back to `routes.account_url`
    - All 5 files use `font-display`, `font-body`, `bg-coral`, `text-deep`, `text-mid` — no inline styles
    - All 5 files render inside `layout/theme.liquid` automatically (no `{% layout %}` override needed — Shopify default)
  </acceptance_criteria>
  <done>All 5 customer account templates render with Soleil Noir brand styling — login, register, account overview with order history, address management, and order detail pages all use design tokens and typography.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| customer Liquid templates → Shopify auth | All auth handled by Shopify — no custom session management |
| Address form → Shopify | `{% form 'customer_address' %}` submits to Shopify; CSRF handled by Shopify form helper |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Spoofing | Customer login | accept | Shopify native auth; passwords hashed by Shopify; no custom credential storage |
| T-02-11 | Tampering | Address form CSRF | accept | `{% form 'customer_address' %}` includes Shopify authenticity_token automatically |
| T-02-12 | Information Disclosure | Order detail page | accept | Shopify gates `/account/orders/:id` — only authenticated customer can view their own orders |
| T-02-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
1. `grep -n "section 'footer'" layout/theme.liquid` returns a match after `</main>`
2. `ls templates/customers/` shows login.liquid, register.liquid, account.liquid, addresses.liquid, order.liquid
3. Visual: `/account/login` renders with brand heading, coral submit button, Soleil Noir nav and footer
4. Visual: `/account` (when logged in) shows order history table or empty state with brand typography
</verification>

<success_criteria>
- Footer renders at bottom of all pages with correct link columns
- Customer login and register pages use brand typography and coral CTA buttons
- Account overview shows order history; addresses page shows saved addresses
- All pages render inside the full theme shell (announcement bar + nav + footer)
</success_criteria>

<output>
Create `.planning/phases/02-global-shell/02-06-SUMMARY.md` when done
</output>
