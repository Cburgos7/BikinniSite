---
phase: 04-collections-product-detail
plan: 05
type: execute
wave: 2
depends_on:
  - 04-04
files_modified:
  - templates/product.json
autonomous: true
requirements:
  - PDP-01
  - PDP-02
  - PDP-03
  - PDP-04
  - PDP-05

must_haves:
  truths:
    - "Navigating to any product URL renders the product-main section"
    - "The product template references product-main as its sole section"
  artifacts:
    - path: templates/product.json
      provides: "Shopify product template wiring product-main section"
  key_links:
    - from: templates/product.json
      to: sections/product-main.liquid
      via: "sections block referencing product-main"
      pattern: "product-main"
---

<objective>
Wire the product template JSON so all product detail pages render via the product-main section built in Plan 04.

Purpose: Shopify requires `templates/product.json` to map /products/* URLs to sections. Per D-11 through D-14, the product-main section contains the complete PDP.
Output: templates/product.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collections-product-detail/04-CONTEXT.md
@.planning/phases/04-collections-product-detail/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create templates/product.json</name>
  <files>templates/product.json</files>
  <read_first>
    - templates/collection.json — same JSON structure pattern used in Plan 03; replicate for product
    - templates/index.json — confirm Shopify template JSON schema (sections object + order array)
    - sections/product-main.liquid — confirm the section `"name"` in schema is "Product Main" and the file slug is `product-main`
  </read_first>
  <action>
    Create templates/product.json following the same structure as templates/collection.json.

    JSON structure:
    - Top level: `{ "sections": { ... }, "order": [ ... ] }`
    - sections object: one key `"product-main"` with value `{ "type": "product-main", "settings": {} }`
    - order array: `["product-main"]`

    The section type value must match the sections/product-main.liquid filename without `.liquid`: `product-main`.

    Do NOT add additional sections — the global layout (layout/theme.liquid) provides nav, cart drawer, and footer automatically via content_for_layout.
  </action>
  <verify>
    File exists at templates/product.json. `grep -c "product-main" templates/product.json` returns >= 2. File is valid JSON.
  </verify>
  <acceptance_criteria>
    - templates/product.json exists
    - `grep -c "product-main" templates/product.json` returns >= 2
    - `grep -c "\"type\": \"product-main\"" templates/product.json` returns 1
    - File is valid JSON with `sections` and `order` top-level keys
  </acceptance_criteria>
  <done>templates/product.json exists and wires product-main section for all product detail page URLs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/product.json → Shopify section renderer | Static config file; no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05-01 | Tampering | templates/product.json | accept | Static JSON committed to repo; no user-controlled data; risk is repo access control only |
</threat_model>

<verification>
- templates/product.json exists and is valid JSON
- Shopify theme preview: /products/[any-product-handle] loads without "template not found" error and renders the product-main section
</verification>

<success_criteria>
- templates/product.json routes all /products/* URLs through product-main section
- PDP-01 through PDP-05 are accessible at product URLs
</success_criteria>

<output>
Create .planning/phases/04-collections-product-detail/04-05-SUMMARY.md when done
</output>
