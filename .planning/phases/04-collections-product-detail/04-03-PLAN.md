---
phase: 04-collections-product-detail
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified:
  - templates/collection.json
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03

must_haves:
  truths:
    - "Navigating to /collections/bikinis renders the collection hero, filter sidebar, and product grid"
    - "Navigating to /collections/lingerie renders the same template with Lingerie content"
    - "Navigating to /collections/all renders the all-products catalog with the same filtering UI"
    - "The collection template references the collection-grid section"
  artifacts:
    - path: templates/collection.json
      provides: "Shopify collection template wiring collection-grid section"
      exports: []
  key_links:
    - from: templates/collection.json
      to: sections/collection-grid.liquid
      via: "sections block referencing collection-grid"
      pattern: "collection-grid"
---

<objective>
Wire the collection template JSON so all collection pages (Bikinis, Lingerie, All-Products) render using the collection-grid section built in Plan 02.

Purpose: Shopify requires a `templates/collection.json` to map the collection URL to sections. Per D-01, one shared template drives all three collections.
Output: templates/collection.json
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-collections-product-detail/04-CONTEXT.md
@.planning/phases/04-collections-product-detail/04-02-SUMMARY.md
@.planning/phases/04-collections-product-detail/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create templates/collection.json</name>
  <files>templates/collection.json</files>
  <read_first>
    - templates/index.json — existing template JSON format to replicate structure (sections object, order array)
    - sections/collection-grid.liquid — confirm section name attribute in schema matches "collection-grid" key used in template JSON
    - layout/theme.liquid — confirm collection template is rendered via content_for_layout (no special wiring needed)
  </read_first>
  <action>
    Create templates/collection.json following the same structure as templates/index.json. The file must reference the `collection-grid` section by its Shopify section key.

    The JSON structure:
    - Top level: `{ "sections": { ... }, "order": [ ... ] }`
    - sections object: one key `"collection-grid"` with value `{ "type": "collection-grid", "settings": {} }`
    - order array: `["collection-grid"]`

    Confirm the section type value matches the section's filename without `.liquid` extension: `collection-grid`.

    The collection.json has no blocks and no additional sections — the collection-grid section contains all layout internally.

    Do NOT add announcement-bar or header sections — those are part of the global theme layout (layout/theme.liquid) and render automatically via `content_for_layout`.
  </action>
  <verify>
    File exists at templates/collection.json. `grep -c "collection-grid" templates/collection.json` returns >= 2 (in sections key and order array). File is valid JSON.
  </verify>
  <acceptance_criteria>
    - templates/collection.json exists
    - `grep -c "collection-grid" templates/collection.json` returns >= 2
    - File passes JSON parse: valid JSON with `sections` and `order` top-level keys
    - `grep -c "\"type\": \"collection-grid\"" templates/collection.json` returns 1
  </acceptance_criteria>
  <done>templates/collection.json exists and wires collection-grid section as the single section on all collection pages.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| templates/collection.json → Shopify section renderer | JSON template is a static config file — no user input, no dynamic content |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | templates/collection.json | accept | Static JSON file committed to repo; no user-controlled data; tamper risk is repo access control only |
</threat_model>

<verification>
- templates/collection.json exists and is valid JSON
- Shopify theme preview: /collections/bikinis loads without "template not found" error
- /collections/lingerie and /collections/all also render the collection-grid section
</verification>

<success_criteria>
- Single templates/collection.json routes all collection URLs through collection-grid section
- COLL-01, COLL-02, COLL-03 satisfied: Bikinis, Lingerie, All-Products all served by this template
</success_criteria>

<output>
Create .planning/phases/04-collections-product-detail/04-03-SUMMARY.md when done
</output>
